# TODO

This document is the current working backlog for `vrm-rs`. Keep detailed history in
`docs/progress.md`; keep this file focused on remaining work, priority, and done
criteria.

## How To Use

- Update this file when a task is completed, split, or superseded.
- Keep external binaries and generated golden files under `.external-fixtures/`; do not commit them.
- Before committing implementation work, run the local Rust CI script:

```powershell
cargo +nightly -Zscript tools/ci/local-ci.rs
```

The repository intentionally has no GitHub-hosted CI. Use `tools/ci/local-ci.rs` for the CI-equivalent local gate and its optional external fixture / render parity passes.

## P0: Bevy Adapter Integration

- [x] Read real Bevy hierarchy into `BevyRuntimeSceneState`.
  - Done when `VrmNode` entities can derive parent/child links from Bevy ECS hierarchy components and spring/constraint/first-person tests no longer need manual `set_parent` setup.
- [x] Make `BevyVrmSpringParityState` recapture rules explicit.
  - Done when model changes, rest-pose changes, spring setup changes, and manual reset are represented by a small API or marker resource instead of requiring callers to remember `clear`.
- [x] Write `BevyVrmMaterialState` into renderer-facing material assets.
  - Done when at least one optional system maps material state into Bevy material assets while preserving the current render-feature-light default path.
- [x] Write morph weights into real Bevy mesh/skinned-mesh state.
  - Done when a Bevy app can drive expression morph targets without reading the lightweight staging component directly.
- [x] Implement Bevy first-person `auto` mesh handling.
  - Done when headless mesh planning can clone or update Bevy mesh assets for first-person view while keeping third-person meshes intact.

## P1: Renderer And MToon Practicality

- [x] Add concrete MToon materialization examples for Bevy.
  - Done when base/outline pass plans, alpha/depth/cull state, render order, emissive strength, and texture refs are demonstrated against Bevy-facing assets or example components.
- [x] Add wgpu and ash integration skeleton examples.
  - Done when renderer owners can map `MtoonPipelinePass`, `MaterialRef`, and `TextureRef` into their own pipeline/material tables without depending on Bevy.
- [x] Keep shader generation out of core crates.
  - Done when any renderer-specific shader work lives in examples, optional adapters, or downstream crates, with `vrm-core` and `vrm-adapter` remaining renderer-agnostic.
- [x] Make engine coordinate systems configurable through typed adapter policy.
  - Done when render diagnostics and runtime-driver integrations can choose VRM/native or mirrored left-handed coordinates through generic marker types without changing `vrm-core` or `vrm-runtime`.
  - `CoordinateSpaceMapping` covers projection helpers and affine matrix remapping, and `CoordinateSpaceTarget` now wraps engine adapters so transform/world/constraint/lookAt I/O is converted at the boundary while non-spatial traits are delegated unchanged. Scene backends that override `SceneGraph::visit_children` can avoid allocating a returned child list on hot child-traversal paths; the default implementation keeps compatibility by delegating to `children()`.

## P1: three-vrm Parity

- [x] Deepen VRM0 numeric humanoid compatibility.
  - Done when Alicia or another VRM0 fixture has golden numeric checks for humanoid axis, rest pose, raw/normalized pose writeback, and legacy orientation behavior.
- [x] Expand VRM0 legacy material edge cases.
  - Done when additional legacy material float/vector/texture-transform cases are covered by generated tests and at least one external fixture assertion.
- [x] Improve spring bone numeric parity tolerance.
  - Done when the collider-heavy fixture tolerance is either tightened or the remaining float-path difference is documented with a focused solver test.
- [x] Add more official spring fixture breadth.
  - Done when directory-level ignored golden tests cover more than Seed-san and the current collider-heavy constraint sample.
- [x] Add fixture-driven node constraint manager parity.
  - Done when complete VRM scenes compare constraint ordering/writeback against three-vrm golden output, beyond standalone quaternion solver cases.
- [x] Broaden VRMA parity.
  - Done when additional `.vrma` samples cover multiple clips and mixed humanoid rotation, hips translation, expression, and lookAt tracks.
  - Normal-gate generated coverage now also applies one sampled frame through humanoid writeback, hips translation, preset/custom expression morphs, and lookAt delivery without external binaries.
- [x] Tighten VRMA diagnostics.
  - Done when unsupported channel/path warnings and errors have stable tests and messages.

## P2: IO, Protocol, And Public API

- [x] Raise `vrm-io` coverage.
  - Done when invalid GLB/glTF, buffer/image/accessor extraction, extension shape, and VRMA error paths are covered enough to materially raise line coverage from the current low-water mark.
- [x] Expand protocol roundtrip coverage.
  - Done when representative unknown extension and `extras` retention tests exist for each major schema family.
- [x] Polish the root facade.
  - Done when the root crate has an ergonomic load/validate/runtime-driver path for common `.vrm`, `.vrma`, `.gltf`, and `.glb` use.
- [x] Add docs.rs-ready examples.
  - Done when facade, sans-IO, runtime driver, and adapter usage have short examples that compile in docs or integration tests.

## P3: Render Parity And External Automation

- [x] Add optional external-fixture local automation.
  - Done when a local Rust script downloads documented external fixtures into `.external-fixtures/official`, generates three-vrm golden files under `.external-fixtures/golden`, and runs ignored fixture/golden tests without committing binaries.
- [x] Expand redistributable official VRMA clip parity.
  - Done when fixture discovery records additional official VRMA clips, their license/provenance status, and semantic or golden parity assertions beyond the current `test.vrma` baseline where redistribution and CI use are acceptable.
  - Current discovery found stable `test.vrma` plus branch-only experimental `idle_loop.vrma`; both remain external-only, and `idle_loop.vrma` now extends hips translation scaling parity. See `docs/vrma-fixture-discovery.md`.
- [x] Add a non-Bevy engine adapter implementation.
  - Done when a concrete renderer-neutral or wgpu-oriented scene/material state implements the adapter traits enough for runtime-driver ticks, material writeback, and render-preparation examples without Bevy.
- [x] Deepen wgpu/ash material pipeline examples.
  - Done when examples map MToon descriptors into concrete pass ordering, alpha/depth/cull state, render order, texture/sampler bindings, uniform layouts, and pipeline/material keys for wgpu-style and ash-style renderers.
  - The default local Rust gate now executes `cargo run --example mtoon_renderer_skeletons`, `cargo run --example wgpu_mtoon_pipeline_materialization`, `cargo run --example ash_mtoon_pipeline_materialization`, `cargo run --example bevy_mtoon_materialization`, and `cargo run --example custom_engine_adapter`, so the renderer-neutral skeleton, wgpu-shaped bind-group/render-pipeline mapping, Vulkan-shaped descriptor/pipeline mapping, Bevy-facing material asset mapping, and a non-Bevy custom-engine runtime adapter flow are checked by `just ci`.
- [x] Add shared MToon GPU ABI and reference shader surface.
  - Done when renderers can consume one renderer-neutral uniform layout and binding-number contract instead of cloning MToon shader parameter packing in each adapter.
  - `vrm-adapter` now exposes `MtoonGpuUniform`, `MtoonGpuMaterial`, `MtoonGpuTextureBindingPlan`, binding-number helpers, `MTOON_GPU_UNIFORM_SIZE`, and `MTOON_REFERENCE_WGSL`. The wgpu adapter exposes `wgpu_mtoon_resource_plans`, the ash adapter emits uniform uploads in `AshRendererFrame`, and Bevy-facing material plans carry the same uniform for engine-owned materials.
- [ ] Raise the operational three-vrm render parity harness to full parity thresholds.
  - Done when the existing three-vrm, Bevy, and wgpu comparison harness reaches final compatibility thresholds across the real and generated fixture set, with PSNR/imqraw metrics plus visual review showing no remaining material, alpha, ownership, normal-map, outline, or animation regressions that affect supported use cases.
  - Current status: the harness is operational. It produces comparable artifacts, direct `.imqraw` numeric reports, review manifests, and visual-review outputs under `.external-fixtures/`; the remaining work is raising real-fixture floors and shrinking the documented Seed-san/local-ownership residuals rather than building the harness from scratch.
  - Latest PBR diagnostic correction: Browser owner projections now dump source-derived `MeshStandardMaterial` diffuse/specular/direct/ambient terms, and Rust CPU hotspot PBR terms now linearize sRGB base textures before evaluating glTF/PBR. Same-surface `backpack_nm` Browser/Rust direct terms now agree in the owner/render join, so do not chase the previous large CPU-term mismatch as a renderer gain problem. Continue with full rendered sample/fill/shader-output residuals for `backpack_nm` plus the separate MToon body/plastic rows.
  - Latest same-surface PBR/output residual update: `join-owner-render-hotspots.rs` now reports Browser-vs-Rust PBR term distances beside final actual-vs-three-vrm output RGB distance for matching surfaces. The regenerated Seed-san owner/render join shows Browser-best to Rust-frontmost total-term distance is tiny across all three Rust renderers (wgpu `0.0047`, Bevy `0.0047`, Ash `0.0005`), while output RGB distance remains much larger (wgpu `52.8983`, Bevy `51.8283`, Ash `32.2958`). Treat this as evidence against another global PBR direct-light/ambient/exposure fit; next work should inspect final shader-output encoding, resolve/sample/fill, or local surface-adoption behavior for those pixels.
  - Latest owner-tail diagnostic status: projection-gap summaries now include 3x3 same-pixel subpixel triangle-containment buckets in addition to pixel-center and pixel-origin checks. The existing `pixel_subpixel3_inside_*` metrics are per-pixel any-hit buckets, and the newer `subpixel3_*_sample_hits` metrics expose the 0..9 sample totals. On the compact Bevy-vs-wgpu topology artifact, `draw-index` leaves `26` conservative unexplained pixels with `173` expected-triangle subpixel hits, `6` actual-triangle subpixel hits, and `4` same-sample hits; uniform owner-color output is identical to vertex-color output. The compact summary now prints the near-depth same/adjacent bucket too, which is `0` for this tail. The diagnostic-only reverse-order matrix separates `draw-index-reverse` (phase offset preserved) from `draw-index-reverse-no-phase` (phase offset removed); both worsen the same artifact to `52` unexplained pixels, and `--owner-id-strict-depth-compare` also worsens it to `52`. The near-ID recovery counters show `46/46` mismatches and `26/26` conservative tail pixels match the expected owner if the actual RGB is decoded as a nearby owner ID, mostly one red-channel LSB (`r-1` / `r+1`) with draw-index deltas of `-1` or `+1`. The comparator now reports `unresolved_owner_tail_after_near_id_mismatched_shared_nonzero` and `unresolved_owner_tail_after_near_id_after_touching_mismatched_shared_nonzero`; the compact Bevy-vs-wgpu topology artifact is `0` / `0` after that reclassification. Keep the historical tail counters for audit, but treat this compact Bevy-vs-wgpu owner-id residual as diagnostic-color quantization/recovery noise rather than an active geometry/fill blocker.
  - Ash comparison status: `--render-ash-readback` now lets the same local runner emit and verify ash `.rgba.json`/`.imqraw`/PNG readback artifacts, compile the source-controlled MToon GLSL handoff into local SPIR-V, compare Ash against the three-vrm reference with the same RGBA/direct-imqraw report writers used by wgpu and Bevy, write an Ash diff heatmap, and list Ash in `review-manifest.json` as a non-gating `comparisons[]` entry with `visualParityGate: false`. The runner forwards camera/light/MToon lighting arguments to Ash, and the Ash projection applies the Vulkan clip-space Y flip required by the shared top-left RGBA artifact convention. `--render-ash-visual-gate` is available as the opt-in switch that applies the normal fail-under/max-delta thresholds to Ash once the real-fixture color and edge residuals are closed. The ash unsafe example now also allocates and writes real MToon uniform buffers, a scene/camera/light uniform buffer, material-extra and UV-transform uniform buffers, sampled texture images, samplers, and descriptor sets, including every TextureRef referenced by the MToon/glTF material texture-slot plan. `--vertex-spv` / `--fragment-spv` can replace the built-in color-only smoke shader with validated external SPIR-V without vendoring shader binaries. The source-controlled `mtoon_base.{vert,frag}.glsl` shader plus `just ash-mtoon-base-readback` prove the fixed slot layout, local SPIR-V compilation, normal/tangent vertex inputs, MToon uniform and texture-slot consumption, glTF emissive/occlusion texture-slot consumption, outline geometry expansion and outline-pipeline routing, scene light/camera uniform consumption, material-extra/UV-transform consumption, external shader module path, Vulkan resources, and raw artifact verification work together. `just render-parity-mtoon-light-ash-generated` now proves the generated MToon light/color fixture against Ash at `rgb-interior1px` `59.7573 dB` with max selected-channel delta `2` and all 12 named swatches within `<= 2`. The source-like transparent blend fixture now proves generated BLEND alpha/RGB ordering across wgpu, Bevy, and Ash with exact alpha buckets and max selected-channel delta `<= 1` against three-vrm. These remove generated light/color accumulation and generated transparent alpha/RGB ordering as current Ash/Bevy blockers, while full Ash visual parity still needs real-model local edge/material residuals and broader fixture coverage raised to the wgpu/Bevy floor.
  - First slices landed: `tools/render-parity/compare-psnr.mjs` and `docs/render-parity.md` define the RGBA artifact format, PSNR report, and initial visual-review thresholds; `tools/render-parity/three-vrm-browser-capture.mjs` captures the three-vrm WebGL reference frame through Chromium, flips WebGL readback rows into top-left order, and can write PNG review artifacts; `vrm-io` exposes mesh primitives, decoded image metadata, texture mappings, glTF PBR base-color texture fallback, and skin inputs for Bevy/wgpu buffer construction; `examples/wgpu_render_capture.rs` writes textured, rest-skinned wgpu RGBA/PNG artifacts from real mesh primitives and applies MToon-derived render order, cull, alpha, blend, depth-write policy, shade color, shading shift/toony, ambient, and effective emissive strength.
  - `tools/ci/local-ci.rs --render-parity` now regenerates three-vrm, wgpu, and Bevy RGBA artifacts under the same canonical opaque-black visual-review background contract, while `--render-background transparent` remains available for alpha-mask audits. It verifies that wgpu/Bevy alpha masks match the three-vrm reference within `--render-alpha-mismatch-tolerance`, writes canonical PNGs encoded from those RGBA artifacts, PSNR reports, amplified RGB/alpha diff heatmaps, `.external-fixtures/render-parity/summary.md`, `.external-fixtures/render-parity/review-manifest.json`, and `.external-fixtures/render-parity/visual-review.html` in one local command. The summary records selected PSNR, max selected-channel delta, alpha mismatch/max delta, and pass/fail per fixture/backend, and is embedded in the visual review page; the manifest links the same gate data to all source, raw, PNG, report, and diff artifacts for audit, and `validate-review-manifest.rs` checks those links, report-local expected/actual image paths, mirrored numeric summary fields, and comparison pass flags before the render-parity run succeeds. It defaults to `Seed-san.vrm`, accepts repeated `--render-fixture NAME.vrm` flags for broader official-sample sweeps, and applies `--render-fail-under` to an explicit `--render-psnr-metric` that defaults to `rgb-visible`. Direct `three-vrm-browser-capture.mjs --png-out` now also encodes PNG from raw RGBA readback data, so the standalone three-vrm preview path no longer depends on browser canvas compositing.
  - `just imqraw-compare-rgba` and `just render-parity-imqraw-seed-normal` provide an independent PNG-free metric path. The TypeScript helper packs existing `.rgba.json` buffers with the `imqraw` JS/WASM library and sends that raw bundle to `imq image`, so reports can confirm the raw buffer metrics without relying on PNG decode/encode behavior.
  - The remaining comparator replacement requirements are captured in `docs/imq-compare-vrm-parity-requirements.md`: move VRM-specific metric domains, one-pixel interior masks, alpha diagnostics, and selected-threshold gates into `imq`, then switch renderers toward direct `imqraw` output.
  - three-vrm, wgpu, and Bevy captures now accept `--imqraw-out`, and the local render-parity runner emits `.frame000.imqraw` artifacts for all three renderers. `tools/render-parity/verify-imqraw-rgba.rs` fails the run if any direct `.imqraw` numeric-gate artifact diverges from its companion `.rgba.json` visual-review artifact. `tools/render-parity/compare-imqraw.rs` decodes those direct bundles through the `imq` Rust crate and writes `.imqraw-rust.json` reports with the same VRM-specific domains as `compare-psnr.mjs`; the main render summary and threshold gate now consume those direct raw reports. The remaining comparator replacement step is to retire the repository-local comparator once the installed public `imq` CLI exposes the required domains and thresholds natively.
  - Bevy capture now has a Bevy 0.18.1 headless offscreen example using real mesh and texture inputs plus shared RGBA/PNG output. The Seed-san PSNR baseline is `28.25 dB` after rest-skinning, MToon-derived alpha/cull/render-order policy, matching the three-vrm/wgpu 30 degree camera projection, expanded outline meshes with outline width texture sampling, reference-exposure correction, glTF emissive factor/strength/texture handling, glTF metallic/roughness extraction with a compact GGX non-MToon fallback, base-plus-one outline ordering, a first custom Bevy MToon material/shader path with base, shade, shading-shift, matcap, rim, normal texture bindings, and lit outline-color mixing, Bevy-side generated tangents for normal-mapped primitives that omit glTF `TANGENT`, mipmapped repeat/linear texture sampling, configurable capture-only MToon lighting coefficients, explicit `Msaa::Off`, shared headless runtime world/skin matrices, double-sided back-face normal/TBN flipping, and resolved MToon inheritance of glTF base/emissive/texture/normal material params to match the antialias-disabled reference condition and adapter runtime path. The wgpu baseline is `28.42 dB` after matching three.js directional-light vector convention, adding expanded outline draws with outline width texture sampling and base-plus-one outline ordering, applying the same correction, binding MToon shade/matcap/shading-shift/rim/normal textures separately from the main texture, using tangent-space normal maps with generated tangents where needed and view-direction matcap UVs, consuming glTF alpha/double-sided/emissive/metallic/roughness material policy, flipping double-sided back-face normal/TBN, using the same compact GGX fallback for non-MToon glTF materials, matching three-vrm's rim/matcap composition and lit outline-color mixing more closely, adding mipmapped repeat/linear texture sampling, retuning the capture lighting defaults without changing the three-vrm reference, inheriting glTF base/emissive/texture/normal params into resolved MToon materials, and using the same shared headless runtime world/skin matrices as Bevy.
  - The broader official-sample render sweep covers `Seed-san.vrm`, `VRM1_Constraint_Twist_Sample.vrm`, `VRMC_materials_mtoon_UV_Animation_Test.vrm`, the mask-heavy `VRMC_vrm_expressions_isBinary_{Overridden,Overrides}.vrm` samples, and the external `UniVRM/AliciaSolid_vrm-0.51.vrm` VRM0 transparent-material fixture; a separate `just render-parity-uv-animation` recipe advances MToon material time to `1.0` for UV scroll/rotation parity. The capture light vector now follows three-vrm's directional-light direction through the VRM orientation compensation, the default capture light/color accumulator is now `three-vrm`, concrete captures now honor glTF sampler min/mag/wrap policy per texture, wgpu binds samplers per material texture slot, both concrete captures share the renderer-neutral `LoadedVrm::material_texture_slots`, `GltfMaterialTextureSlots::binding_plan`, `LoadedVrm::material_uv_transforms`, `GltfMaterialUvTransforms::uniform_plan`, `LoadedVrm::material_shading_plan`, `GltfMaterialRenderExtraPlan`, `GltfPrimitiveData::transformed_vertices`, `GltfPrimitiveData::outline_vertices`, `vrm-adapter::renderer_material_pipeline_plan`, `GltfSkinData::joint_matrices`, `vrm-io::skin_vertex`, and `vrm-io::skin_direction` resolvers, `vrm-io::transform_tex_coord_0`, `vrm-adapter::MtoonLightingConfig`, `vrm-io::image_data_to_rgba8` / `vrm-io::image_bytes_to_rgba8` source normalization helpers, `vrm-io::CpuRgba8Image` repeat/linear CPU sampler, and `vrm-io::generate_rgba_mip_chain` mip helper. The renderer-facing glTF path extracts `KHR_materials_unlit` for PBR fallback materials, while the concrete captures keep VRMC MToon materials on the MToon shader branch even when glTF unlit is present to match the official UV-animation sample's three-vrm behavior. Current selected PSNR in `.external-fixtures/render-parity/summary.md` is Seed-san wgpu `34.6181 dB` and Seed-san Bevy `34.0835 dB`, and the six-fixture sweep passes the current `34 dB` visible-surface floor with exact alpha parity under the canonical opaque-black review background. The next parity blocker is raising the real-fixture floor beyond this baseline and reducing the remaining high max-channel silhouette/material deltas.
  - `just render-parity-samples-nonblack` now runs the same six real fixtures with selected `rgb-nonblack-interior1px >= 27.4 dB`, ignoring opaque-black background pixels and one-pixel silhouette edges so the score tracks model-body color parity rather than canvas background or raster fill-rule noise. `just render-parity-samples-nonblack-ash-gated` applies the same gate with Ash included. The current Ash-gated object-body floor is Seed-san Bevy `29.4512 dB`; Seed-san wgpu is `30.1229 dB` and Ash `30.1042 dB`, constraint is around `29.72-29.74 dB`, UV animation is around `29.56-29.61 dB`, expression samples are above `50 dB`, and Alicia VRM0 is wgpu/Ash `34.4442 dB` / Bevy `34.3709 dB`.
  - `just render-parity-real-normal-maps` now keeps a focused review directory for the unique real tangentless normal-map fixtures reported by `inspect-mtoon-fixtures`: Seed-san has `6` normal-mapped primitives without tangents and `VRM1_Constraint_Twist_Sample.vrm` has `3`. `just render-parity-real-normal-maps-ash-gated` enforces the same selected `rgb-visible >= 34 dB` and exact alpha parity with Ash included: Seed-san wgpu `34.6538 dB` / Bevy `34.1163 dB` / Ash `34.6391 dB`, and constraint sample wgpu `36.2518 dB` / Bevy `36.2349 dB` / Ash `36.2509 dB`. `just render-parity-normal-maps-off` disables normal maps across three-vrm, wgpu, and Bevy for the same fixtures and passes a `35 dB` floor: Seed-san improves to wgpu `36.0858 dB` / Bevy `35.3848 dB`, while constraint remains roughly flat at wgpu `36.2203 dB` / Bevy `36.2054 dB`. The concrete capture paths now share `vrm-io::GltfNormalMapPlan` for authored tangents, generated-tangent fallback, derivative fallback, and normal-scale delivery, and Bevy fills missing generated tangent vertices with `vrm-io::fallback_tangent` instead of dropping normal maps for the whole primitive, so future normal-map parity changes happen through a tested renderer-neutral decision point.
  - `just render-parity-outline-off` disables MToon outline expansion across three-vrm, wgpu, and Bevy for diagnosis. Against the current three-vrm reference it now stops on Seed-san Bevy at `33.2973 dB` while wgpu remains above the floor at `34.4716 dB`; this confirms Bevy's current Seed-san blocker is not only outline expansion. Older constraint-sample diagnostics still showed outline expansion as a concrete contributor for that fixture.
  - `just render-parity-real-transparent` covers the same six real external fixtures on transparent background. It now uses selected `rgb-all` PSNR with fail-under `32`, while alpha mismatches are checked separately with tolerance `64`: Seed-san wgpu/Bevy `25/32`, constraint `11/11`, UV animation `0/0`, expression mask samples `3/3`, Alicia VRM0 `32/32`. Current selected PSNR passes that floor with the exact light accumulator and glTF sampler policy: Seed-san wgpu `34.5645 dB`, Bevy `34.0434 dB`; constraint wgpu `36.2028 dB`, Bevy `36.1877 dB`; UV animation wgpu `35.2517 dB`, Bevy `35.2073 dB`; expression masks around `55.2-55.7 dB`; Alicia wgpu `35.6238 dB`, Bevy `35.6088 dB`.
  - License-safe generated transparent fixtures are available through `just render-parity-transparent-generated`, `just render-parity-transparent-high-contrast`, `just render-parity-transparent-broad`, `just render-parity-transparent-texture-transform`, `just render-parity-transparent-queue-matrix`, `just render-parity-transparent-alpha-modes`, `just render-parity-transparent-mask-texture`, and `just render-parity-transparent-depth-stack`, with `*-ash-gated` variants for the base, high-contrast, broad, texture-transform, queue-matrix, alpha-mode, mask-texture, depth-stack, and lighted cases. They write source-like VRM1 glTF files under `.external-fixtures/generated/`, render on a transparent background, and now gate both selected PSNR and bounded worst-case deltas through `--render-max-selected-channel-delta` / `--render-max-alpha-delta`. They currently verify exact alpha-bucket parity for overlapping MToon `BLEND` primitives (`transparent=512 opaque=0 partial=65024`) across three-vrm, wgpu, Bevy, and Ash where the Ash-gated variants exist. The normal fixture verifies transparent RGB accumulation with an embedded bufferView PNG base-color texture and enforces selected `rgb-visible >= 49 dB`, max selected channel delta `<= 2`, and exact alpha; the Ash-gated run gives wgpu `54.3997 dB`, Bevy `56.8605 dB`, and Ash `54.3997 dB`. The high-contrast Ash-gated fixture passes at wgpu `52.5258 dB`, Bevy `51.7899 dB`, and Ash `52.5258 dB`; broad passes at wgpu `53.8478 dB`, Bevy `49.9717 dB`, and Ash `53.8478 dB`; texture-transform passes at wgpu `53.4205 dB`, Bevy `49.0270 dB`, and Ash `53.4205 dB`; queue-matrix passes at wgpu `53.0342 dB`, Bevy `48.4839 dB`, and Ash `53.0342 dB`; depth-stack passes at wgpu `54.3614 dB`, Bevy `50.6890 dB`, and Ash `54.3614 dB`; alpha-modes pass at `Infinity` for all three Rust renderers with exact alpha; and mask-texture passes at wgpu `55.3599 dB`, Bevy `51.2036 dB`, and Ash `55.3599 dB`. The mask-texture fixture includes `COLOR_0` to guard the spec/three-vrm behavior that MToon ignores vertex colors while renderer IO still extracts them for non-MToon materials.
  - VRM0 MToon compatibility now follows more of three-vrm's v0 compat conversion for gamma-corrected colors, shade shift/toony normalization, GI equalization, centimeter outline width, outline lighting mode, alpha keywords, transparent Z-write, UV animation Y scroll, transparent/transparent-Z-write render queue remapping, and the `V0_COMPAT_SHADE` direct-light clamp.
  - `just render-parity-transparent-lighted` now adds source-like overlapping transparent MToon layers that combine texture alpha, `transparentWithZWrite`, direct/shade/rim lighting, and emissive texture strength; `just render-parity-transparent-lighted-ash-gated` includes Ash in the same visual gate. The current Ash-gated run passes at wgpu `53.7519 dB`, Bevy `50.2049 dB`, and Ash `53.7519 dB`, with max selected channel delta `<= 3` and alpha max delta `<= 2`.
  - wgpu/Bevy MToon shader policy now avoids the incorrect main-texture fallback for missing `shadeMultiplyTexture` and applies matcap texture transforms to sphere UVs like three-vrm.
  - `--mtoon-light-accumulation three-vrm` now uses the reference-shaped exposure `1.0`, ambient irradiance `pbrAmbient`, no GI ambient proxy, and direct-plus-indirect rim accumulator, and is the default for concrete captures and broader PSNR sweeps. `tuned` remains available only as an explicit diagnostic mode. The public renderer-neutral MToon ABI now exposes `MtoonLitShadeInputs`/`mtoon_lit_shade_rate`, and `MTOON_REFERENCE_WGSL` is aligned with the same three-vrm `linearstep(-feather, feather, dotNL + shift)` toon-ramp equation used by the concrete wgpu, Bevy, and Ash capture shaders.
  - A source-like generated MToon light/color fixture is available through `just render-parity-mtoon-light-generated`. It covers forced base, forced shade, three-vrm-compatible ignored MToon `occlusionTexture`, parametric rim, matcap rim, mixed rim/matcap, angled-normal toon-ramp quads, mid-ramp interpolation quads, pure `emissiveFactor`, `emissiveTexture` multiplied by `KHR_materials_emissive_strength`, and a branch guard where `VRMC_materials_mtoon` and `KHR_materials_unlit` are both present without committing binary assets. The recipe uses transparent background plus `rgb-interior1px` so the selected score measures swatch interior color rather than the known raster edge delta; current selected PSNR is wgpu `60.0462 dB` and Bevy `51.4259 dB`, with max selected channel deltas `1` and `2`. It also writes per-swatch color reports with `tools/render-parity/compare-swatch-colors.rs`, currently enforcing every wgpu and Bevy swatch at max channel delta `<= 2`; the `mtoon-plus-khr-unlit` swatch passes at wgpu `62.2143 dB` and Bevy `53.1816 dB`. `just render-parity-mtoon-light-ash-generated` runs the same fixture with supplemental Ash readback and Ash swatch reporting; current Ash selected `rgb-interior1px` is `60.0462 dB`, max selected-channel delta is `1`, and all 13 Ash swatches pass `<= 2`, including `mtoon-plus-khr-unlit` at `62.2143 dB`. `just render-parity-mtoon-light-direct-generated` disables ambient on both sides and still passes at wgpu `59.7409 dB` / Bevy `51.6508 dB`, keeping direct-light color parity separate from the default ambient `0.1` reference setup. `just render-parity-mtoon-light-colored-generated` adds a non-white direct-light guard (`1.0, 0.55, 0.25`) and passes at wgpu `59.5993 dB` / Bevy `51.9037 dB`, proving direct diffuse and rim-light color accumulation are not only matching for white lights. `just render-parity-mtoon-light-scaled-colored-generated` now uses `--render-sync-three-vrm-light-units` to derive Rust `direct-light-scale` and `pbr-ambient` from non-default three-vrm light intensities; the scaled colored guard passes at wgpu `61.9831 dB` / Bevy `51.6960 dB` with max selected channel deltas `2` / `2`. `just render-parity-mtoon-light-ambient-generated` disables directional light on both sides through the three-vrm reference intensity and Rust direct-light scale controls, and passes at wgpu `61.7386 dB` / Bevy `54.5071 dB`. `just render-parity-mtoon-post-correction-generated` covers linear-to-sRGB, overbright clamp, emissive strength clamp, and transparent post-correction interiors with `rgb-visible-interior1px`, passing at wgpu `52.8818 dB` / Bevy `54.1650 dB`. All focused light/post-correction recipes enforce aggregate selected-channel delta `<= 2` and keep their report directories separate, so exact direct/colored-direct/scaled-colored/ambient/post-correction regressions do not hide behind broader render PSNR.
  - A source-like generated MToon texture-slot fixture is available through `just render-parity-mtoon-textures-generated`. It covers `shadingShiftTexture`, `rimMultiplyTexture`, `uvAnimationMaskTexture`, and `outlineWidthMultiplyTexture` at `mtoon-time=1.0` with exact alpha parity and selected `rgb-interior1px` PSNR wgpu `53.5556 dB` / Bevy `50.9425 dB`.
  - The texture-slot fixture now also covers `shadeMultiplyTexture` and enforces max selected channel delta `<= 8`; current selected `rgb-interior1px` PSNR is wgpu `53.8749 dB` / Bevy `51.2935 dB` with exact alpha.
  - A separate opt-in generated MToon normal-map fixture is available through `just render-parity-mtoon-normal-generated`. It adds a tangentless normal-textured MToon swatch without committing binary assets, passes exact alpha parity, and currently reports selected `rgb-interior1px` PSNR wgpu `47.8977 dB` / Bevy `46.7852 dB` after wgpu and Bevy moved to the shared `vrm-io::generate_tangents` helper. The recipe now enforces `rgb-interior1px >= 46.5 dB`. The three-vrm reference rejected the synthetic normal swatch when a `TANGENT` attribute was present with a `vTangent` WebGL validation error, so this fixture intentionally exercises the tangentless fallback path; the Bevy path preserves generated tangents for referenced vertices even when the accessor includes unreferenced vertices.
  - A source-like generated screen-coordinate outline fixture is available through `just render-parity-screen-outline-generated` and `just render-parity-screen-outline-generated-ash-gated`. It covers `outlineWidthMode = screenCoordinates` against three-vrm/wgpu/Bevy/Ash with selected `rgb-opaque` PSNR wgpu `Infinity`, Bevy `53.2689 dB`, and Ash `Infinity`; the known one-pixel fill-rule alpha edge mismatch is tracked separately (`188`, tolerance `256`).
  - A source-like generated base-texture UV-boundary fixture is available through `just render-parity-texture-boundary-generated` and `just render-parity-texture-boundary-generated-ash-gated`. It covers embedded PNG base-color texture sampling across a simple generated UV discontinuity with exact alpha parity, selected `rgb-shared-nonblack-interior1px` PSNR wgpu `52.9232 dB` / Bevy `49.5132 dB` / Ash `52.9232 dB`, and max selected-channel deltas `7` / `6` / `7`, so the remaining Seed-san base-texture blocker is not a generic sampler or generated UV seam failure.
  - A source-like generated per-material base texture selection fixture is available through `just render-parity-texture-selection-generated` and `just render-parity-texture-selection-generated-ash-gated`. It covers four MToon materials selecting four distinct embedded PNG base textures with exact alpha parity, selected `rgb-shared-nonblack-interior1px` PSNR wgpu `58.0021 dB` / Bevy `52.5998 dB` / Ash `58.0021 dB`, and max selected-channel deltas `1` / `2` / `1`, making broad base texture binding-table mistakes unlikely as the remaining Seed-san blocker.
  - A source-like generated same-material UV-island ownership fixture is available through `just render-parity-uv-island-ownership-generated`. It covers three overlapping `huku_bake` mesh nodes that share one MToon material while sampling separated high-gradient base-texture islands. The current base-color run passes exact alpha parity with selected `rgb-shared-nonblack-interior1px` PSNR wgpu `57.3121 dB` / Bevy `51.2217 dB` and max selected-channel delta `3`; hotspot summaries keep all `32/32` top pixels on `huku_bake`, all actual colors closer than three-vrm to the CPU-sampled frontmost base texture, and all local gradients above `96`. `just render-parity-uv-island-ownership-owner-generated` has exact wgpu-vs-Bevy owner parity, only `8/28554` shared nonzero owner mismatches against three-vrm for each Rust renderer, and zero unexplained owner tail after adjacent/touching projected-triangle classification. `just render-parity-uv-island-ownership-owner-generated-ash-gated` now adds Ash to the same owner-id visual gate at selected `rgb-visible` PSNR `92.0359 dB`, max selected-channel delta `1`, and exact alpha for wgpu/Bevy/Ash. `just render-parity-uv-island-ownership-owner-hotspots` reports all `32/32` rendered owners at frontmost rank 1 at the center sample, so the remaining Seed-san blocker is not generic same-material UV-island/high-gradient sampling by itself.
  - A source-like generated same-material subpixel ownership fixture is available through `just render-parity-subpixel-ownership-generated`. It covers material `huku_bake`, near-subpixel seams, and a high-contrast base texture with exact alpha parity, selected `rgb-shared-nonblack-interior1px` PSNR wgpu `43.8282 dB` / Bevy `43.4195 dB`, and max selected-channel deltas `161` / `160`. The new `just render-parity-subpixel-ownership-owner-generated`, `just render-parity-subpixel-ownership-owner-generated-ash-gated`, and `just render-parity-subpixel-ownership-owner-hotspots` recipes add owner-id image comparison and three-vrm owner hotspot projection for the same fixture. The Ash-gated owner run has exact alpha parity and selected `rgb-visible` PSNR `82.3745 dB` for wgpu/Bevy/Ash with max selected-channel delta `6`; the 2026-06-19 owner run has exact wgpu-vs-Bevy owner parity, only `2/18276` shared nonzero owner mismatches against three-vrm for each Rust renderer, all `32/32` projected top hotspots on `huku_bake`, `28/32` center-frontmost owner matches, and `32/32` recovery to frontmost rank 1 through subpixel or one-pixel-neighbor search. The remaining Seed-san blocker is therefore more specific than a small generated subpixel seam by itself.
  - A source-like generated dense ownership fixture is available through `just render-parity-dense-ownership-generated`, `just render-parity-dense-ownership-owner-generated`, and `just render-parity-dense-ownership-owner-generated-ash-gated`. It covers four same-material `huku_bake` mesh nodes with many close/overlapping same-depth triangles and a high-gradient embedded base texture. This exposed and fixed a Bevy shaded OPAQUE source-order gap: Bevy now keeps shader alpha opaque but routes opaque MToon through the source-order-capable render path. The shaded dense gate now passes exact alpha with selected `rgb-shared-nonblack-interior1px` PSNR wgpu `39.6165 dB` / Bevy `39.4014 dB`, max selected-channel delta `172`, and wgpu-vs-Bevy `51.3683 dB` with max delta `2`. The Ash-gated owner run passes `rgb-visible >= 49 dB`, max selected-channel delta `48`, exact alpha, exact wgpu/Bevy/Ash owner parity, and only `37/22455` shared nonzero owner mismatches against three-vrm with unexplained tail `29`. This closes a concrete Bevy source-order regression while keeping the remaining Seed-san blocker classified as more specific than generated dense same-material topology.
  - Concrete wgpu/Bevy outline expansion now follows three-vrm's local/object-normal offset after morph/skin position deformation. The focused real constraint sweep stayed at wgpu `36.2028 dB` / Bevy `36.1877 dB`, so the remaining outline residual is more likely raster/fill or material-side than that transform branch.
  - A source-like generated expression fixture is available through `just render-parity-morph-expression-generated` and `just render-parity-morph-expression-generated-ash-gated`. It passes `happy=1.0` through three-vrm's `expressionManager`, wgpu, Bevy, and Ash, covers non-zero morph target expression writeback plus material `color` and texture-transform binds through the shared `LoadedVrm::expression_render_effects` plan, uses `GltfPrimitiveData::morphed_vertex` for renderer-neutral local morph evaluation, and currently reports selected `rgb-interior1px` PSNR wgpu `58.2703 dB` / Bevy `50.8123 dB` / Ash `58.2703 dB`; the known edge-only alpha mismatch is `3` pixels with tolerance `8`.
  - `tools/render-parity/inspect-mtoon-fixtures.rs` and `just inspect-mtoon-fixtures` now inventory MToon feature coverage from local fixtures without committing binary samples. The current official fixture set contains no screen-coordinate outline materials, but does cover world outline, transparent-ZWrite, UV animation, normal textures, matcap textures, and shade textures; generated fixtures provide the missing screen-outline, strict transparent-ZWrite, texture-slot, alpha-mode, and tangentless normal-map guards. The scanner also shows `21` official normal-mapped primitives without glTF `TANGENT`, making three-vrm's derivative normal-map fallback a concrete follow-up candidate; `just render-parity-normal-maps-derivative` now measures that fallback explicitly but remains diagnostic because the current derivative path is worse than generated tangents on Seed-san (`30.8717 dB` wgpu / `30.6436 dB` Bevy). `just render-parity-normal-maps-view-derivative` adds a closer view-space derivative tangent-frame diagnostic, but the first 64px Seed-san smoke still measured below the generated-tangent path at wgpu `29.5312 dB` / Bevy `29.5376 dB`. The local runner also exposes `--render-normal-map-scale` as a Rust-renderer-only diagnostic knob; the default remains `1.0`, and Seed-san checks at `0.75` and `1.25` both worsened PSNR, keeping the focus on tangent-frame/sampling parity rather than a scalar normal-strength retune.
  - Remaining parity blockers: runtime/material breadth on broader real model fixtures beyond the covered reference-shaped accumulator, raising object-body `rgb-nonblack-interior1px` scores beyond the current Seed-san/UV-animation regression floor, improving real outline expansion for the constraint sample, raising real tangentless normal-map thresholds beyond the current `34 dB` real-fixture floor, real transparent-fixture visual parity beyond the current regression floors, finding or legally adding a real screen-coordinate outline fixture if one becomes available beyond the generated guard, and higher PSNR thresholds. `just render-parity-imqraw-seed-normal-deltas` confirms Seed-san's worst remaining normal-map sweep deltas are RGB-only visible/interior body pixels rather than alpha mismatches. `just render-parity-seed-flat-diagnostic` further splits that blocker: flat white model-body interiors now match exactly (`rgb-nonblack-interior1px = Infinity`, `0` interior-nonblack changed pixels), while the remaining flat deltas are only silhouette/raster-edge pixels. `just render-parity-seed-base-factor-diagnostic` then proves material assignment/base color factors match in the overlapping body region (`Infinity` wgpu, `74.8665 dB` Bevy), while the current `just render-parity-seed-base-color-diagnostic` reports wgpu `32.4602 dB` / Bevy `32.4177 dB`. `just render-parity-seed-base-color-outline-off-diagnostic` now verifies that disabling outlines leaves the focused base-color floor essentially unchanged at the same wgpu `32.4602 dB` / Bevy `32.4177 dB`, with exact alpha parity and top-64 hotspot pass ownership all on base surfaces; the Rust-side hotspot reports now use glTF material names such as `huku_bake`, `arm_mat`, and `backpack_nm` through `LoadedVrm::material_display_name`. The stricter two-pixel body mask only improves the older slice to wgpu `30.0100 dB` / Bevy `28.8790 dB`, a no-mip three-renderer diagnostic worsens to wgpu `28.5038 dB` / Bevy `27.6273 dB`, and a Rust-only V-flip diagnostic worsens to wgpu `10.9506 dB` / Bevy `10.9418 dB`; `just render-parity-seed-uv-diagnostic` and `just render-parity-seed-base-uv-diagnostic` both report wgpu `30.3816 dB` / Bevy `30.1784 dB`, so base texture transform and MToon UV animation are not the Seed-san cause. `just render-parity-seed-base-color-raw-srgb-diagnostic` is now available as an explicit Rust-only experiment, but it is slightly worse than the normal path on Seed-san (`32.1018` / `32.0621 dB`) and generated base-texture guards continue to pass only on the normal default path, so a broad raw-base filtering switch is not the fix. The hotspot mapper now samples frontmost base texture RGBA directly and applies mask-alpha visibility. All-pixel reports still expose silhouette/raster edge ownership, but the focused `shared-nonblack-interior1px` top-64 reports have center visible candidates for every hotspot and show Rust actuals closer to the CPU-sampled frontmost texture than three-vrm expected. Browser-side three-vrm projection now reaches the same conclusion: CPU frontmost base color is closer to Rust actual than rendered three-vrm expected (`52.06` vs `115.12` mean RGB distance), while the expected color is often a different nearby candidate surface. The browser `owner-id` diagnostic is sharper: WebGL renders a nonzero owner for every focused hotspot, but that owner appears in the CPU center-sample candidate set only `20/64` times at `0.5,0.5` and `18/64` times at `0.75,0.75`, with owner/frontmost matches only `14/64` and `17/64`. A 3x3 same-pixel subpixel recovery only reaches `34/64` rendered owners, all frontmost, and a 3x3 one-pixel-neighborhood recovery reaches `31/64` (`30/64` frontmost), with mixed offsets rather than one global direction. Broad V-flip, mip, binding-table, alpha-mask, simple backface-cull, raw base texture filtering, outline expansion, and center-coverage recovery failures are unlikely for the shared-body PSNR floor. Rust wgpu/Bevy now also emit per-triangle owner IDs in concrete diagnostic mode; the latest owner-id run improves three-vrm comparison to wgpu `16.6869 dB` / Bevy `16.7007 dB`, with wgpu-vs-Bevy still close at `62.1850 dB` and max channel delta `2`. The owner-id comparator now exposes exact ID offset clusters, transition pixel bounds/sample pixels, labeled metadata details, projected screen bounds, depth, and pass-transition counts. The next ownership step is shrinking or explaining the real-material local surface/triangle-order clusters around named regions like `huku_bake` and `arm_mat` before returning to MToon light/normal/rim/shade accumulation. Edge/raster differences remain tracked separately.
  - Latest owner-id clarification: the prior large `outline -> base` owner mismatch was a browser-reference diagnostic artifact. Seed-san uses overlapping base/outline material groups over the same index range; the browser capture now duplicates all attributes and morph attributes per material group before assigning owner colors. With that fix, default CCW is the useful diagnostic: wgpu reports `base -> base = 11911`, `outline -> outline = 18`, and `outline -> base = 4`. The CW recipe now worsens as expected (`base -> outline = 11913`, selected owner-ID PSNR wgpu `14.4732 dB` / Bevy `14.4760 dB`) and should stay a negative diagnostic. `top_render_policy_transitions` carries expected and actual effective cull/front-face fields, and `top_actual_cull_visibility` confirms wgpu has `0` actual-not-visible owner pixels in both default and CW reports. Bevy still reports a separate actual-not-visible bucket (`2436` default / `1014` CW), but the metadata-bounds recovery diagnostic explains the default CCW split as owner diagnostic ID/fill behavior: wgpu-vs-Bevy has `4498` actual metadata bounds misses and all `4498` recover to nearby owner IDs whose projected bounds contain the pixel; Bevy-vs-three-vrm is `4492/4492`. The geometry-class summary further reduces the clean wgpu owner blocker: `11649/11933` wgpu-vs-three-vrm mismatched pixels are actually the same projected triangle, `11866/11933` are same, adjacent, or shared-edge projected triangles, and the new unexplained tail is only `67/11933`. Owner records now expose normalized `renderPhaseOrder` only for MToon/ShaderMaterial owners; current wgpu-vs-three-vrm `base -> base` MToon owner mismatches are mostly same phase (`11123`), while the former `788` one-phase bucket is now correctly classified as `unknown -> unknown` non-MToon/PBR renderer queue context. Use `top_render_phase_order_transitions` for real MToon queue parity, use raw renderer `renderOrder` only as local context, and focus next on the tiny non-overlap / different-triangle / different-pass owner tail plus broader shaded PSNR.
  - Latest outline-off base-texture blocker update: a Rust-side screen-jitter sweep now accepts negative clap values and shows `0,0` remains the best global raster position; `-0.25/0.25` offsets lower the focused PSNR, so do not pursue a global pixel-center retune. The outline-disabled owner diagnostic reports that wgpu-vs-three-vrm still has `11933` shared owner mismatches, but `11631` are same projected triangle, `11848` are same, adjacent, or shared-edge projected triangles, and the unexplained tail is only `85` pixels after per-owner `indices` topology classification. The new `top_unexplained_material_transitions` groups that tail by named material/mesh: the largest clusters are same-material different-triangle ownership on `huku_bake`, `hair`, `body_nm`, `body_bake`, `arm_mat`, and `arm_plastic`, plus a small outline/base `huku_bake` cluster. Bevy now emits finite `referenceWebglDepth` beside its actual infinite reverse-Z owner depth; the refreshed outline-off report has `7292/12140` Bevy-vs-three-vrm mismatches in strict same/adjacent/shared-edge projected classes and an unexplained tail of `539`, so its former depth-convention blocker is largely normalized. Next work should build or find a smaller real-topology reproduction for Seed-san-style split meshes/material groups before trying more MToon light/color accumulation changes.
  - `just render-parity-seed-owner-tail-outline-off-summary` now turns those large owner reports into compact ignored artifacts for review. Current summary paths are `.external-fixtures/render-parity-seed-owner-id-outline-off-diagnostic/reports/Seed-san.wgpu-vs-three-vrm.owner-tail.summary.md`, `.external-fixtures/render-parity-seed-owner-id-outline-off-diagnostic/reports/Seed-san.bevy-vs-three-vrm.owner-tail.summary.md`, and `.external-fixtures/render-parity-seed-owner-id-outline-off-diagnostic/reports/Seed-san.bevy-vs-wgpu.owner-tail.summary.md`. Latest compact counts: wgpu-vs-three-vrm tail `85` / after-touching `80`, Bevy-vs-three-vrm `539` / `149`, and Bevy-vs-wgpu `477` / `99`. Use these summaries to pick the next source-like reproduction target, starting with the `wear_4 -> wear` `huku_bake` and shared-vertex Bevy local ownership clusters.
  - A source-like indexed shared-vertex ownership fixture is available through `just render-parity-shared-vertex-ownership-generated`, `just render-parity-shared-vertex-ownership-owner-generated`, and `just render-parity-shared-vertex-ownership-owner-generated-ash-gated`. It covers one `wear` mesh, material `huku_bake`, triangle fans/strips with real shared indices, and a high-gradient texture. The base-color gate passes at wgpu `48.8813 dB` / Bevy `47.3812 dB`, and the Ash-gated owner-id path passes at wgpu/Bevy/Ash `61.8661 dB` with exact alpha. Owner reports show `0` Bevy-vs-wgpu and `0` Ash-vs-wgpu shared owner mismatches, with only `21/21763` shared owner mismatches against three-vrm for each Rust renderer. This narrows the Bevy shared-vertex tail away from generic indexed shared-vertex handling and toward the real Seed-san material grouping/fill-rule case.
  - A source-like same-mesh primitive-group ownership fixture is available through `just render-parity-primitive-group-ownership-generated`, `just render-parity-primitive-group-ownership-owner-generated`, and `just render-parity-primitive-group-ownership-owner-generated-ash-gated`. It keeps one `wear` mesh with two same-material primitives sharing indexed positions/normals but using separate high-gradient UV groups. The base-color gate passes at wgpu `55.5458 dB` / Bevy `50.5968 dB`, exact alpha, and max selected-channel delta `5`; the Ash-gated owner-id gate passes at wgpu/Bevy/Ash `71.1946 dB`, max delta `25`, exact wgpu-vs-Bevy and Ash-vs-wgpu owner parity, and only `9/23722` shared owner mismatches against three-vrm, all adjacent/touching with zero unexplained tail. This narrows the remaining Seed-san tail away from generic same-mesh multi-primitive grouping.
  - Bevy owner-id diagnostics now explicitly unindex meshes in index-buffer order, matching the wgpu capture path and avoiding reliance on Bevy's generic `Mesh::try_duplicate_vertices`. The Seed-san outline-off rerun remains unchanged at Bevy-vs-wgpu tail `477` / after-touching `99`, so the remaining Bevy owner tail is not diagnostic vertex-duplication order; keep focus on raster/depth/fill behavior around real shared-edge triangles.
  - Bevy owner metadata now reports actual material front-face and reverse-Z `greater-equal` depth compare, and owner-tail markdown now includes actual-not-visible and metadata-bounds miss/recovery counts. The current Seed-san outline-off Bevy-vs-wgpu compact summary exposes `2445` mismatched pixels whose actual owner metadata says cull-invisible and `4509/4509` metadata-bounds misses recovered by nearby owner ID. The next concrete blocker is therefore Bevy projection/cull metadata versus actual pipeline behavior, then a smaller shared-edge raster/fill fixture if the metadata proves correct.
  - Bevy render capture no longer adds a large per-draw-order depth-bias term after sorting transparent-phase diagnostic MToon primitives. Treat draw-order depth-bias amplification as ruled out: Bevy's reported diagnostic depth bias is now around `0.000019` instead of about `0.0089`, and `just render-parity-split-ownership-owner-generated` still has exact Bevy-vs-wgpu owner parity. The topology owner-tail guard now exposes bounds-edge, inside/outside, edge-side, and small-bounds shape metrics. On the latest artifact, Bevy-vs-three-vrm has `46` unexplained gaps with `34` within `0.5px` of either owner bounds edge and `11` involving an owner bounds area `<= 4px`; Bevy-vs-wgpu has `41` unexplained gaps with `31` near a `0.5px` bounds edge, `10` involving `<= 4px` bounds, `23` inside the wgpu expected bounds only, and no Bevy-actual-only pixels. Next work should inspect Bevy/three-vrm/wgpu coverage and sort behavior at those small-triangle edge pixels rather than sampler, broad material, or global depth policy.
  - Bevy owner-id diagnostics now include `--disable-owner-id-depth-bias` and `--disable-owner-id-phase-order`, with applied phase offsets recorded per owner and carried through the owner comparison/summary reports. On the topology extract, depth-bias-only removal is neutral, phase-order removal improves Bevy-vs-wgpu unexplained gaps `41 -> 35`, and removing both improves them to `31`; Bevy-vs-three-vrm improves `46 -> 40 -> 36`. `just render-parity-seed-owner-tail-topology-current-phase-summary` refreshes the current Bevy owner-id phase/sort slice without regenerating the whole topology sweep. With the current `draw-index` default, the latest summary reports Bevy-vs-wgpu tail `26`, so the remaining compact artifact is now a local coverage/fill residual rather than a broad phase-order hook.
  - The remaining owner-id generated guards reject global Bevy owner-id phase suppression or broad scale reduction. Full no-bias keeps subpixel, shared-vertex, split, and front-face exact, but regresses primitive-group, dense, and uv-island. A smaller `1e-8` phase offset keeps topology improved and primitive-group exact, but still regresses dense (`5913` mismatches), uv-island (`9594`), and split (`1728`). The `--owner-id-phase-order-policy overlap-area` experiment also fails as a default candidate: it reduces topology tail pixels with a nonzero actual Bevy phase offset (`40 -> 32`) but does not reduce the unexplained tail (`40/41 -> 41`). The stricter `overlap-triangle` diagnostic improves the topology tail to `35` but breaks generated source-order guards (dense `5883`, split `1728` Bevy-vs-wgpu mismatches; UV-island remains exact), so it is also diagnostic-only. The implemented `draw-index` policy is now the owner-id default and replaces Bevy's diagnostic `Transparent3d.distance` with `(render_order, draw_index)`: primitive-group, dense, UV-island, split, shared-vertex, subpixel, and front-face/cull all stay exact against wgpu, while the compact topology tail drops to `26`. The `draw-index-no-depth` diagnostic is rejected too: dense regresses to `1287` mismatches and topology tail worsens to `54`. The latest summary adds center/opposite-bounds distance and pixel-origin counts: center sampling has `23/26` expected-only pixels, `22/23` of them within `2px` of actual bounds, while origin sampling still has `16/26` expected-only and `9/26` neither-inside. Next work should focus on local Bevy coverage/fill or bounds-generation behavior around close same-material same-triangle owners, not broad phase/depth suppression or a pure center/origin metadata convention change.
  - The owner-tail summary now also reports raster pixel bounds and raster-to-metadata alignment against owner metadata bounds. The current topology fixture keeps wgpu clean (`0` owners with center-bounds excess and `0` owners over `2px` / `4px` metadata-alignment drift) but Bevy has `14` owners with center-bounds excess, worst excess `19.55310059px` at owner `90`, plus `5` owners / `22` pixels over `2px` and `3` owners / `12` pixels over `4px` self-center drift. Bevy owner-id output now renders one triangle per draw, encodes the owner ID in vertex color, zeroes the material owner uniform, and disables blend; three-vrm, wgpu, and Bevy all emit `ownerColorSource`, and summaries preserve it as `owner_color=vertex-color` in Bevy detail rows. Owner-id entities opt out of automatic batching and owner-id captures force GPU preprocessing to `None`; the topology tail remains `26`, while split/dense/shared-vertex/primitive-group generated owner guards still pass. Treat uniform owner-color binding, vertex-color interpolation, owner-id blending, Bevy batching, and indirect preprocessing as ruled out, and continue with real-model local Bevy coverage/fill or material-primitive assignment diagnostics.
  - Latest topology containment diagnostic: `compare-owner-id-images.rs` now preserves owner `screen` triangles and reports center/origin point-in-triangle counts for tail pixels. In the compact Bevy-vs-wgpu topology run, `26/26` tail pixels are inside the wgpu expected triangle at center and `0/26` are inside the Bevy actual triangle at center; origin sampling is `12/26` expected and `0/26` actual. This makes the residual look less like a bounds/fill-reporting convention and more like Bevy emitting a neighboring owner ID at another triangle's raster location.
  - Owner metadata now includes `sourceTriangle` for all three capture paths. Bevy reports comparison-facing `triangle` and `sourceTriangle` as the pre-split source ordinal, while preserving the generated split-mesh ordinal as `bevyLocalTriangle`; wgpu/three-vrm mirror `triangle` and `sourceTriangle`. The refreshed full Seed-san outline-off owner diagnostic uses release-built Bevy because debug Bevy can time out on the one-triangle-per-draw path, and it reports wgpu-vs-three-vrm tail `85` / after-touching `80`, Bevy-vs-three-vrm `539` / `149`, and Bevy-vs-wgpu `477` / `99`. The owner recovery-delta table shows many Bevy recoveries are one-LSB neighboring IDs (`r+1` / `g+1`), but a direct positive UNORM output bias worsened the full fixture and was reverted. The Bevy uniform owner-color diagnostic exactly matches the vertex-color counters, so owner-color transport is also ruled out. The next useful owner work is therefore not metadata source/local triangle alignment, vertex/uniform color transport, or a global color bias; it is the remaining Bevy local fill/material ownership behavior where actual owner IDs are recoverable by nearby same-mesh/same-material metadata owners with source/draw deltas like `+1` and `+256`.
  - A source-like front-face/cull ownership fixture is available through `just render-parity-front-face-cull-owner-generated` and `just render-parity-front-face-cull-owner-generated-ash-gated`. It renders opposite-winding triangles under one back-culling MToon material and gates wgpu, Bevy, and Ash owner-id captures against three-vrm at `rgb-visible >= 90 dB`, selected-channel delta `<= 1`, and exact alpha. The latest Ash-gated run reports wgpu/Bevy/Ash `102.3162 dB` overall PSNR, `0` shared owner mismatches, `0` actual-not-visible-by-cull-policy mismatches, and `0` metadata bounds misses. This rules out simple front-face/cull policy drift as the Seed-san Bevy owner-tail cause and keeps the remaining blocker focused on dense real-model local primitive/fill ownership.
  - A source-derived Seed-san owner-tail extraction diagnostic is available through `just render-parity-seed-owner-tail-extract`, `just render-parity-seed-owner-tail-context-extract`, and `just render-parity-seed-owner-tail-topology-extract`. It uses `tools/render-parity/extract-owner-tail-fixture.rs` to read the outline-off Seed-san owner report, resolve top unexplained base-pass labels back to the source VRM, write reduced normalized VRM1 glTF fixtures under `.external-fixtures/generated/`, and keep source node/mesh/material/triangle metadata in root `extras`. The isolated gate passes at selected `rgb-visible` wgpu/Bevy `72.3162 dB`, max selected-channel delta `16`, and exact alpha with `47/47` exact shared owner matches against three-vrm for both wgpu and Bevy. The index-window context gate uses `--context-radius 3`, emits `109` source-neighborhood triangles, and passes at wgpu `85.8817 dB`, Bevy `85.6261 dB`, max selected-channel delta `4`, exact alpha, and only `3` wgpu / `5` Bevy owner mismatches out of `249` shared nonzero pixels against three-vrm. The topology context gate uses `--context-shared-vertex-depth 2`, emits `200` same-primitive graph-neighborhood triangles, and is now mainly a local owner-tail classifier: wgpu-vs-three-vrm has `6` unexplained gaps, Bevy-vs-three-vrm has `46`, and Bevy-vs-wgpu has `41`, with most Bevy residuals near a `0.5px` bounds edge or involving `<= 4px` owner bounds. This gives the remaining Bevy/raster-fill ownership tail a compact source-derived guard while preserving the earlier negative controls.
  - Latest split-ownership fixture update: `just render-parity-split-ownership-generated` now gives a license-safe same-material overlap guard for the `huku_bake` / `wear_4 -> wear` class, while `just render-parity-split-ownership-owner-generated` confirms wgpu and Bevy agree on owner IDs after the Bevy capture queues diagnostic OPAQUE MToon through ordered `Transparent3d`. `just render-parity-split-ownership-owner-generated-ash-gated` adds Ash to that owner-id visual gate at selected `rgb-visible` PSNR `96.2956 dB`, max selected-channel delta `1`, and exact alpha for all three Rust renderers. Real shaded `BLEND` materials keep the normal transparent sort. This closes the Bevy-specific equal-depth diagnostic ordering regression in the small fixture and keeps Ash covered on the same owner-control path. `summarize-render-hotspots.rs` now emits compact hotspot summaries for the small fixture and Seed-san outline-off run; the current Seed-san summary shows `58/64` focused hotspots within `0.25px` of an edge and Rust actuals closer to the CPU frontmost base texture for `43/64` hotspots. Remaining work is therefore the broader real-model local primitive/UV/fill ownership tail, especially where three-vrm owner choice diverges from CPU frontmost texture candidates.
  - Latest flat-interior diagnostic update: `rgb-shared-nonblack-flat32-interior1px` and its complement `rgb-shared-nonblack-gradient-interior1px` are available in `compare-imqraw.rs`, `compare-psnr.mjs`, `inspect-imqraw-deltas.rs`, and `tools/ci/local-ci.rs`. The flat32 metric keeps shared non-black one-pixel interiors only when both images have low local RGB gradients, making it a high-signal diagnostic for stable material/texture interiors. `just render-parity-seed-base-color-flat32-outline-off-diagnostic` currently reports Seed-san base-color/outline-off flat32 PSNR wgpu `49.7607 dB` and Bevy `47.5483 dB`, compared with the ordinary shared-body score around `32.4 dB`; the gradient complement is wgpu `26.3277 dB` / Bevy `26.3322 dB` with max deltas around `219`. Do not promote flat32 to the main compatibility gate by itself; use the flat-vs-gradient split to prove whether future changes improve true interior color or only move local edge/fill-rule pixels. The next concrete blocker remains real-model local primitive/material/UV ownership around dense texture gradients.
  - Latest gradient hotspot update: the flat32 diagnostic recipe now writes `*.hotspots.gradient.summary.{json,md}` with same-triangle, edge-neighbor, base-UV distance, and CPU frontmost texture local-gradient fields. Current gradient summaries have `64/64` visible frontmost candidates, `58/64` within `0.25px` of an edge, all top hotspots in the base pass, Rust actual closer than three-vrm expected to CPU frontmost texture for `44/64` hotspots, only `6/64` same-triangle matches, `0/64` edge-neighbor matches, and large mean UV distance from the frontmost candidate. The CPU frontmost base texture local RGB gradient is low despite the rendered gradient-domain selection (wgpu mean/max `4.7577` / `25.4951`, Bevy `4.6618` / `25.4951`, and `0/64` hotspots at `>=32`), so the next implementation target stays on local UV/material/triangle ownership around real Seed-san material regions rather than broad sampler, texture binding, alpha, outline, MToon lighting, or same-surface high-frequency texture filtering changes.
  - Source-derived gradient hotspot extraction is now available through `just render-parity-seed-gradient-hotspot-extract`. The extractor can consume `map-render-hotspots.rs` output directly and preserve frontmost / nearest actual / nearest expected / nearest-sample candidate triangles plus shared-vertex context. The first run emitted `214` triangles and reduced the artifact to wgpu `50.9391 dB` / Bevy `49.8443 dB` on `rgb-shared-nonblack-interior1px`, max selected-channel delta `5` / `6`, exact alpha, with only `11` wgpu and `26` Bevy changed hotspot pixels. This means the CPU-frontmost/nearest candidate path does not reproduce the large Seed-san gradient floor by itself.
  - Direct three-vrm owner-hotspot extraction is now available through `just render-parity-seed-owner-hotspot-extract`. The extractor consumes `reference.renderer.diagnosticHotspots`, preserves rendered/frontmost/subpixel/neighbor recovered owner labels, and resolves them back to source primitives rather than diagnostic split indices. The first base-color run emitted `148` triangles and reduced the artifact to wgpu `65.4948 dB` / Bevy `52.9020 dB` on `rgb-shared-nonblack-interior1px`, max selected-channel delta `1` for both, exact alpha, `5` wgpu changed shared-interior pixels, and `63` Bevy changed shared-interior pixels. `just render-parity-seed-owner-hotspot-owner-extract` adds a thresholded owner-id guard for the same reduced fixture across wgpu, Bevy release, and Ash: wgpu-vs-three-vrm has `3` mismatched shared owner pixels with `2` unresolved after near-ID, Bevy-vs-three-vrm has `53` mismatches but only `2` unresolved after near-ID, and Bevy-vs-wgpu has `51` mismatches with `0` unresolved after near-ID. The owner-label path is now gated; remaining work is broader real Seed-san overlap/material-context extraction or full-model fill-rule analysis, not another broad renderer plumbing pass.
  - Broader owner-hotspot depth3 extraction is now available through `just render-parity-seed-owner-hotspot-depth3-extract` and `just render-parity-seed-owner-hotspot-depth3-owner-extract`. These recipes regenerate the upstream flat32 gradient deltas and browser owner-hotspot projection before extraction so they are not tied to a stale projection file. The extraction preserves three shared-vertex rings around the direct owner-hotspot labels, emits `556` source-derived triangles, and reproduces the low real-model gradient-floor shape at base-color selected PSNR wgpu `31.1507 dB` / Bevy `31.1385 dB`. The base recipe emits thresholded hotspot summaries that keep the frontmost base-texture local gradient bucket at `gte32 = 0` and max local gradient `<= 8`, so this guard continues to prove the compact residual is local ownership/fill/UV context rather than high-frequency texture filtering. The compact summaries now also preserve nearest-sample frontmost counts, offsets, CPU base-color distances, texture sampling variant distances, subpixel frontmost search, fixed subpixel sample rankings, depth-near later-draw source-order classification, and actual-vs-expected surface relation counts; current depth3 runs have nearest-sample visible coverage for every selected hotspot (`24/24` wgpu and `64/64` Bevy), `0` missing-center recoveries, same pass/material for all selected hotspots, and same triangle for wgpu `24/24` plus Bevy `63/64`. Current CPU base-color distances match base-texture distances exactly: wgpu mean actual/expected is `0.6199` / `11.6477` with `12/12/0` actual/expected/tie closer counts, and Bevy is `1.4457` / `4.2341` with `2/59/3` closer counts. That rules out missing baseColorFactor or PBR fallback vertex-color multiplication for this reduced fixture. The sampling variant diagnostic rules out a simple origin/filter/half-texel policy switch: BottomLeft variants are far worse, current `linear_top_left_half_texel` remains closest to Rust actual, and nearest/no-half TopLeft does not move samples toward three-vrm expected. The 5x5 subpixel frontmost search does move strongly toward three-vrm expected: mean expected CPU base-color distance is now `0.1250` for wgpu-vs-three-vrm and `0.0156` for Bevy-vs-three-vrm. The newest split shows those improvements are mostly same-material/pass local fill cases rather than broad surface changes: wgpu improved subpixel matches split into actual `6` same-triangle / `4` different-triangle and expected `8` / `2`, while Bevy splits into actual `34` / `10` and expected `4` / `1`; only tiny expected-side hair-to-arm_mat transitions appear in the transition tables. The fixed-sample ranking points to `[0.5, 0.7]` as the best expected-side sample for both wgpu and Bevy, but direct capture jitter experiments are not good enough to adopt globally: `+0.2px` worsens selected PSNR to about `25.1 dB`, while `-0.2px` leaves PSNR essentially flat around `31.1 dB` despite lowering max selected-channel delta to `155-156`. Depth-near later-draw source-order is also not enough: the default `1e-5` epsilon finds no candidates, and a `1e-3` probe finds only wgpu `1` / Bevy `5` later candidates with zero expected improvements and mean expected distances above `265`. The thresholded owner-id guard includes Ash and shows Bevy-vs-wgpu `277` shared owner mismatches with `0` unresolved after near-ID, so the current reduced fixture points at local subpixel coverage/fill behavior rather than absent center coverage, material-factor loss, simple texture sampling policy, global camera jitter, depth-quantized later-draw source-order, or a wgpu/Bevy/Ash split. This depth3 fixture is now the best compact target for the next parity improvement.
  - Latest depth3 fill diagnostic: strict frontmost inclusion (`min_barycentric >= 0`) now runs beside the existing loose CPU inclusion and reports strict-vs-loose transitions. The current run has no strict/loose surface changes and no actual/expected improvements (`24/24` wgpu and `64/64` Bevy strict frontmost candidates match the loose same material/triangle), so the CPU mapper's loose outside-triangle epsilon is ruled out. Next work should model the actual WebGL/GPU subpixel fill convention or derive a smaller fill-rule fixture from depth3, not retune the CPU diagnostic epsilon.
  - Latest depth3 coverage predictor: the mapper now also averages the visible 5x5 subpixel frontmost CPU colors without using the target image as an oracle. That simple coverage/AA-style resolve is not the missing model: wgpu mean actual/expected distance is `6.8040` / `7.1696`, Bevy is `7.8230` / `6.8783`, and only `3` expected hotspots improve in each comparison. This is much weaker than the target-color best-subpixel oracle and worsens Bevy relative to center frontmost, so the next implementation target should be deterministic local sample-position or fill-ownership selection, not coverage averaging.
  - Latest browser owner projection update: `three-vrm-browser-capture.mjs --hotspot-subpixel-steps 5` and `just render-parity-seed-base-color-flat32-gradient-owner-hotspots-5x5` now align the browser rendered-owner recovery grid with the Rust 5x5 mapper. The 5x5 owner projection still recovers `49/64` rendered owners, all frontmost rank `1`, but dominant centers are `0.5,0.5` (`27`) and `0.7,0.5` (`11`); `0.5,0.7` appears only once. Therefore the Rust target-color best sample `[0.5,0.7]` should not be promoted as a global render sample. Next work should join the browser rendered-owner candidate details with the Rust CPU hotspot candidate details per pixel to identify which local owner/material/triangle choice is missing.
  - Latest owner/render join update: `tools/render-parity/join-owner-render-hotspots.rs` and `just render-parity-seed-base-color-flat32-gradient-owner-render-join` now join the browser owner projection to Rust hotspot candidates by pixel. Current Seed-san flat32 gradient counts are wgpu `64/64` joined and Bevy `63/64` joined. WebGL rendered owners match Rust center frontmost `27/64` wgpu and `26/63` Bevy, but only match Rust expected-best-subpixel `21/64` and `21/63`; browser best-subpixel recovery repeats those counts. This means the expected-color oracle and actual WebGL ownership are not the same model. Next work should split the fix into an owner/fill-rule model and a texture/base-color color model, then test each independently on the compact depth3 fixture.
  - Latest depth3 owner/render join update: `just render-parity-seed-owner-hotspot-depth3-owner-render-join` now runs browser owner projection and join reports for the compact depth3 fixture with separate wgpu/Bevy delta inputs. Here owner and color oracle do align: WebGL rendered owners match Rust expected-best-subpixel wgpu `24/24` and Bevy `64/64`, while Rust center-frontmost is only off by one (`23/24`, `63/64`). This makes depth3 the right target for owner/fill-rule refinement. The broader Seed-san flat32 gradient set remains the target for texture/base-color modelling because there WebGL ownership and expected-color oracle diverge.
  - Latest owner/sample color-distance update: the owner/render join now measures the Rust CPU base color at the browser best owner/sample. Full Seed-san flat32 gradient browser best-sample colors are closer to expected than actual for wgpu `34/49` and Bevy `34/48`, with mean RGB distances around expected `51-53` vs actual `119`; depth3 is nearly exact on expected, with mean expected distances wgpu `0.5823` and Bevy `0.0846`. Next implementation target should be a renderer-side owner/sample policy experiment on depth3, followed by a full Seed-san texture/base-color audit for neighboring surfaces with similar expected colors.
  - Justfile alignment: `just render-parity-current-blocker` is now the intended local entrypoint for the current blocker, and it runs the compact no-readback render-resolve path rather than the value-copy correction path. It refreshes the depth3 owner/fill fixture, emits source-like owner/sample geometry manifests from three-vrm rendered owner IDs and Rust subpixel geometry, renders wgpu/Bevy/Ash through resolve draws, compares direct `.imqraw` artifacts, and verifies each raw artifact against its RGBA JSON companion. `just render-parity-current-blocker-owner-sample-selection` is the default manifest-generation path for owner/fill-rule work. `just render-parity-current-blocker-owner-sample-correction` and `just render-parity-seed-owner-hotspot-depth3-corrected-readback` remain upper-bound diagnostics. Bevy render-parity capture is release-built by default in `tools/ci/local-ci.rs`; use `--render-bevy-debug` only for a deliberate debug-path diagnostic.
  - Latest relation-bucket update: browser best owner/sample choices now report same-surface / same-material-different-triangle / different-material buckets against Rust expected-best. Depth3 is entirely same-surface for both wgpu and Bevy. Full Seed-san expected-closer browser colors split as same-surface `18`, same-material different-triangle `15`, and different-material `1` for both wgpu and Bevy. Treat local same-material sample/triangle ownership as the next implementation target before spending more time on broad cross-material shader color accumulation.
  - Latest raw correction update: `just render-parity-current-blocker-owner-sample-correction` applies only browser owner/sample CPU colors that move pixels closer to three-vrm. Depth3 all-RGB PSNR improves from about `47.75` to `49.50 dB`; full Seed-san flat32 gradient improves wgpu `34.5265 -> 35.3685 dB` and Bevy `34.5145 -> 35.3539 dB` by correcting `31` local pixels, all same-surface or same-material different-triangle. Treat this as an upper-bound diagnostic, not as the default fix: current renderer work must make the browser-selected sample geometry own the pixel before material evaluation.
  - Latest Sans I/O update: owner/sample correction relation and RGB-distance policy now live in `vrm-adapter` as `RenderOwnerId`, `RenderPixel`, `RenderRgba8Correction`, `RenderOwnerSurfaceKey`, `RenderOwnerSurfaceRelation`, `RenderSamplePoint`, `RenderOwnerSampleKey`, `RenderOwnerSampleCorrectionPolicy`, `RenderOwnerSampleCorrectionCandidate`, `RenderOwnerSampleCorrectionDecision`, `RenderOwnerSampleCorrectionPlan`, `RenderOwnerSampleSelectionPlan`, `RenderOwnerSampleSurfaceOverride`, typed RGBA8 apply errors, strict manifest parsing, surface coverage reporting, and the correction evaluators/applicator. wgpu, Bevy, Ash, the raw correction tool, browser/Rust owner-render join, and owner-id comparator consume this shared API, including owner-id color encoding, near-ID recovery/channel-delta policy, renderer-facing correction decisions, source-like JSON correction manifests, in-memory RGBA8 application, and subpixel sample matching epsilon. Correction manifests must carry required `surface` and `sample` metadata beside each `rgba` replacement; raw-array roots, non-schema field names such as `replacementRgba` or `relationToExpected`, and entries without owner/sample source metadata are invalid manifest shapes. `RenderOwnerSampleCorrectionPlan` exposes pixel lookup, surface/sample matching, manifest-vs-render-surface coverage, surface-grouped selection plans, and per-surface override iterators so backend experiments can consume the typed plan earlier in draw/sample selection without reading manifest-entry internals. `vrm-adapter-wgpu`, `vrm-adapter-bevy`, and `vrm-adapter-ash` now turn those selections into concrete backend records at `RENDER_OWNER_SAMPLE_OVERRIDE_BINDING = 20`, including geometry-bearing draw keys, barycentric sample data, raw/base UVs, and relation metadata. The readback RGBA applicator still exists only as an explicit upper-bound diagnostic.
  - Latest Ash owner/sample update: Ash now carries owner/sample selections through `AshRendererFrame` as per-pipeline `OwnerSampleOverride` storage-buffer uploads and point-list resolve draws, and the unsafe device example writes those storage-buffer descriptors beside its existing uniform and sampled-texture descriptors. The old backend-port task is complete for wgpu, Bevy, and Ash; the remaining parity target is broader real-fixture owner/color modelling and exact local edge/material residual cleanup.
  - Latest owner/sample ownership update: wgpu, Bevy, and Ash now use the same no-readback sample-ownership model. Geometry-bearing selection records become one point vertex per selected pixel, using the record's pixel, raw UV, barycentric coordinates, and interpolated primitive attributes, and those points are drawn after the normal pass with the same material bind group or descriptor set. This keeps `replacement_rgba` out of the shader path and makes the browser-selected surface actually own the pixel before material evaluation, instead of relying on a same-surface UV-offset heuristic or final readback patching. The standard selection manifests are now generated by `build-owner-sample-selection.rs` from three-vrm rendered owner IDs plus Rust subpixel geometry only; expected/actual color images are not inputs. wgpu now keeps source world position separate from target clip position and carries source-triangle raw-UV gradients through a packed 16-attribute / 15-inter-stage-location ABI, so the resolve pixel shades from the source surface while landing on the browser-selected target pixel. On compact depth3 base-color, the current render-resolve recipe reports selected `rgb-shared-nonblack-interior1px` PSNR wgpu `64.3542 dB`, Bevy `55.9954 dB`, and Ash `64.3542 dB`; all three have selected max channel delta `1`, alpha mismatches `0`, and `.imqraw` artifacts verified against companion `.rgba.json` artifacts. On the real Seed-san flat32/gradient blocker, the owner-id based manifest path resolves wgpu `49/64` and Bevy `48/63` rendered-owner samples and reports wgpu `28.5090 dB`, Bevy `27.2176 dB`, and Ash `30.7519 dB`, with exact opaque alpha and raw/RGBA verification. The lower wgpu/Bevy score versus the earlier correction-derived run is intentional: the default path is no longer allowed to choose samples because their CPU colors are closer to the target. `just render-parity-seed-base-color-flat32-render-resolve-hotspots` shows the remaining post-resolve top-64 hotspots are still edge-local, low-texture-gradient, and subpixel-predictable, but this is diagnostic evidence rather than a license to pick samples by PSNR. Remaining follow-up: match three-vrm rendered owner/fill-rule behavior first, then audit texture/base-color color modelling and add targeted matcap or UV-animation-mask gradient modelling only if shaded/mipped reports make them measurable blockers.
  - Latest post-resolve owner/root-cause update: `just render-parity-seed-base-color-flat32-render-resolve-owner-render-join` now reruns owner-id projection for the post-resolve residual pixels themselves, then joins those browser-rendered owner IDs with Rust hotspot candidates for wgpu, Bevy, and Ash. A later review rejected treating recovered sample points as a PSNR-driven default; the corrected default is now `--selection-mode webgl-raster-owner`, which uses exact center owner matches first and otherwise uses the browser-rendered owner's pixel-square coverage point. This is still owner-id/fill-rule driven and never reads expected/actual RGB. The current standard current-blocker recipe regenerates the required `.s5` owner-hotspot projection before consuming it; the latest real Seed-san manifests select wgpu `64/64` (`33` center + `31` coverage) and Bevy `63/63` (`32` center + `31` coverage), with `0` fixed-grid subpixel fallbacks. No-readback render-resolve now reports selected gradient PSNR wgpu `36.5003 dB`, Bevy `36.1436 dB`, and Ash `36.9516 dB` with exact opaque alpha. `audit-center-owner-misses.rs` remains a diagnostic explaining why strict pixel-center ownership alone is insufficient. Next work: audit material/texture color at the WebGL-raster owner samples, especially coverage-derived same-material edges, before returning to broader MToon accumulation.
  - Latest coverage/fill audit update: `map-render-hotspots.rs` now reports per-candidate pixel-square coverage area/point count and a RGB-independent `largest_coverage_visible` candidate, and `three-vrm-browser-capture.mjs` records browser coverage area for rendered-owner recovery. `join-owner-render-hotspots.rs` now compares browser `bestCoverage` against Rust frontmost/expected-best/actual-best and resolves the browser coverage sample color from Rust coverage candidates. On the existing Seed-san flat32 gradient artifacts, Rust largest coverage exists for `64/64`; only `12/64` are the same triangle and `38/64` the same material as center-frontmost, all center-frontmost hits are within `1px` of an edge, and depth-near-later remains `0`. Browser best-coverage exists for `64/64`, matches Rust expected-best in `21/64`, and its coverage sample color is expected-closer in `44/64`. Keep the next renderer change tied to a fill/coverage ownership model, not PSNR-driven sample selection.
  - Latest center-shading implementation update: `build-owner-sample-selection.rs` now enforces the documented split for `webgl-raster-owner`: browser coverage chooses the rendered owner surface, while Rust uses the coverage candidate's pixel-center `center_candidate` as the shader/material geometry whenever it is available. The generated Seed-san manifests now report center shading geometry wgpu `64/64` and Bevy `63/63`. A refreshed render-resolve run stays at wgpu `30.6336 dB`, Bevy `28.2142 dB`, and Ash `35.8902 dB` for the gradient selected metric, so the code now matches the intended model and the remaining residual should be treated as broader post-resolve owner/fill coverage plus texture/base-color modelling rather than coverage-centroid sampling.
  - Latest post-resolve selection expansion update: `tools/render-parity/merge-owner-sample-selection.rs` and `just render-parity-seed-base-color-flat32-render-resolve-expanded-selection` now build source-like owner/sample manifests for the post-resolve residual pixels and merge them with the initial blocker manifests without reading target/reference colors. The merge is pixel-keyed, keeps the initial manifest by default, and reports duplicates/conflicts separately. The current run produces post-resolve selections wgpu `64/64`, Bevy `64/64`, and Ash `64/64`; merged manifest sizes are wgpu `101`, Bevy `93`, and Ash `99`, and the expanded coverage audits cover all post-resolve top-64 residual pixels. Next work is to feed these expanded manifests through renderer resolve A/B runs and inspect the remaining shaded/base-color deltas, rather than selecting samples by PSNR.
  - Latest expanded readback A/B update: `just render-parity-seed-base-color-flat32-render-resolve-expanded-readback` now reruns the normal blocker, builds expanded post-resolve manifests, renders them into a separate readback directory, and writes a generated summary via `just render-parity-seed-base-color-flat32-render-resolve-expanded-summary`. The current selected gradient metric improves wgpu `30.6300 -> 32.7302 dB` and Bevy `28.2142 -> 32.4826 dB` after routing Bevy resolve gradients through standard UV0/UV1 attributes, while Ash remains `35.8901 dB`; alpha remains exact. The expanded hotspot summaries still show `64/64` frontmost-visible pixels, `63/64` within `0.5px` of an edge, and no local texture gradients `>= 32`. The expanded texture audit shows wgpu still has `25/64` new residual pixels outside the expanded manifest and Bevy has `24/64`, while Ash has `0/64`; selected buckets are mostly same-material and edge-local. Next work should make the owner/fill model stable across shifted wgpu/Bevy residuals, then inspect selected same-material shaded/base-color evaluation. Do not return to RGB-distance sample selection.
  - Latest color-fit / shader-branch diagnostic update: real Seed-san join Markdown and expanded summary JSON both retain additive/gain `color_fit` values; the parser treats explicit `color_fit: null` as absent and continues to `color_fit_summary` / alias fields, so the machine-readable summary does not lose the join's fit values. Renderer material-draw artifacts now also expose `materialExtra.shaderBranch` and summaries print `branch:gltf_pbr|mtoon|unlit` instead of the older ambiguous `pbr:*` compact label. The full expanded readback artifacts have been refreshed, so focused rows now show current branch labels such as `backpack_nm branch:gltf_pbr` and MToon rows as `branch:mtoon`.
  - Latest browser-compatible base-color projection update: `three-vrm-browser-capture.mjs` now emits `projectedBrowserBaseColor*` and `nearestRenderedBrowserBaseColor` aliases for the MeshBasicMaterial diagnostic readback path, and `join-base-color-owner-hotspots.rs` prefers those aliases while retaining fallback reads for old `projectedBaseColorTextureAsLinear*` artifacts. Treat the older texture-as-linear wording as a historical field name only; it is a diagnostic comparison of browser-compatible base-color projection, not a default Rust renderer color-space policy.
  - Latest second-frontier update: `just render-parity-seed-base-color-flat32-render-resolve-expanded2-selection` builds a second source-derived post-resolve frontier from the expanded residuals without reading expected/actual colors. It adds wgpu `25` and Bevy `21` new target pixels with no merge conflicts, while Ash adds `0`. `just render-parity-seed-base-color-flat32-render-resolve-expanded2-readback` proves this is not a safe default fix: selected gradient PSNR worsens from wgpu `32.7302 -> 30.9642 dB` and Bevy `29.2208 -> 28.6200 dB`; Ash is unchanged at `35.8901 dB`. The layered audit now splits carried/new/missing selection buckets and shows new wgpu selections favor actual over expected `11/1` and new Bevy selections `8/0`, so the added frontier mostly reproduces Rust's current material/base-color result rather than three-vrm's. Treat further blind iteration as rejected; next work is selected-surface material/base-color evaluation, starting with Ash's fully covered bucket and the wgpu/Bevy carried/new edge buckets.
  - Latest material bucket update: `audit-texture-sampling-parity.rs` now emits both frontmost `material_buckets` and manifest `selection_material_buckets` for each all/selected/missing/carried/new layer, including per-material best sampling modes for actual and expected colors. It also reports nearest-expected candidate CPU-color counters, frontmost base-texture counters, best texture-sampling distance thresholds, and signed `actual - texture` / `expected - texture` RGB deltas so we can separate same-material triangle/fill choice from material color evaluation. The selected-surface bucket split shows the manifest-selected material distribution currently matches the hotspot frontmost distribution for selected pixels, so this is not a surface metadata read bug. The nearest-expected split shows Bevy selected `huku_bake` changes from frontmost `16/0` actual/expected closer to nearest-expected `2/14`, wgpu `body_nm` changes from `3/4` to `2/5`, and Ash `body_nm` changes from `4/4` to `2/6`; those should stay in the local fill/triangle ownership bucket. The frontmost base-texture split is identical to the CPU base-color split for the main selected blocker materials, so baseColorFactor and vertex-color multiplication are not the current explanation. `backpack_nm` remains actual-closer at the texture level (`19/0` wgpu and Bevy, `23/0` Ash), and best local sampling variants do not get close to expected (`0` rows <= 8 for wgpu/Bevy/Ash). Bevy `backpack_nm` is essentially the sampled texture on Rust actual (`A-T = 0.00,0.21,0.11`) but much darker than three-vrm expected (`E-T = 56.74,63.00,66.42`), while wgpu/Ash `backpack_nm` actuals are already brighter than texture and expected is brighter still. Split the next work into exact `backpack_nm` texture/color/light accumulation vs `body_nm`/plastic local fill ownership. Keep rejecting a global sampler, mip, UV-origin, surface metadata read bug, RGB-distance selection, or blind owner-frontier iteration.
  - Latest material-probe recommendation update: `audit-texture-sampling-parity.rs` now emits `recommended_probes` from manifest/sample distances and prints them near the top of the Markdown report. On the existing expanded Seed-san readback artifacts, `backpack_nm`, `eye`, `arm_mat`, `huku_bake`, and `arm_plastic` are classified as `selected_sample_and_renderer_both_far` across the main backends, while `body_nm` is `selected_sample_matches_three_vrm` on wgpu/Bevy and `mixed` on Ash. Use this split for the next fixes: first inspect selected-surface material/resolve inputs for the both-far buckets, and separately verify that backend resolve draws actually adopt the selected `body_nm` surface before shading. Do not treat this as permission to reintroduce RGB-distance sample selection.
  - Latest selection-source bucket update: `audit-texture-sampling-parity.rs` now splits selected rows by manifest `selection_source`, so center and WebGL-coverage selections can be diagnosed without reading target/reference colors. On the expanded Seed-san audits, wgpu center rows are `33` with manifest A/E/T `20/13/0`, Bevy center rows are `34` with `22/12/0`, and Ash center rows are `58` with `43/15/0`; all three share the same `webgl-coverage` bucket of `6` rows with manifest A/E/T `2/4/0`, mostly `arm_plastic`, `backpack_nm`, `body_nm`, and `eye`. Treat coverage rows as "WebGL owner was selected, inspect material/color evaluation", and center rows as the larger owner/fill/sample modelling bucket. This is a safer split than changing default sampling based on PSNR.
  - Latest manifest-response diagnostic update: `audit-texture-sampling-parity.rs` now emits actual/expected-over-manifest RGB ratios and least-squares no-intercept RGB gains, and `summarize-render-resolve-expanded.rs` now keeps those fields in the generated top-level summary JSON/Markdown. This is the answer to the current "match behavior, do not fit blindly" concern: it makes the response model observable without turning additive/gain values into renderer knobs. On current expanded Seed-san artifacts, `backpack_nm node145/mesh4/prim9/base` is consistently multiplicative-over-manifest for both Rust actual and three-vrm expected, but expected is brighter than all Rust backends (`E/M` about `2.11,2.36,2.43` vs actual `1.74-1.98`). `body_nm`/plastic rows do not follow the same model, so keep them on the local owner/sample/MToon adoption track. Also keep the `summary.json color_fit` parser guard in place: explicit `color_fit: null` must fall through to populated alias fields, and generated summaries should not emit `color_fit: null` for rows whose join Markdown has additive/gain fits or detailed audit LS gains.
  - Latest missing-response update: the texture audit and expanded summary now emit `least_squares_expected_minus_actual_over_manifest_rgb_gain`, i.e. the extra three-vrm output over Rust actual normalized by the selected manifest sample. Current `backpack_nm node145/mesh4/prim9/base` values are stable across backends: wgpu `0.37,0.46,0.49`, Bevy `0.36,0.45,0.47`, Ash `0.34,0.42,0.44`. Treat this as evidence for a shared PBR light/fill/ambient response gap, not as a post-process exposure/gain knob. Next implementation should compare the Rust PBR direct/ambient terms against three.js MeshStandardMaterial's directional and ambient-light accumulation for this draw, ideally by adding a source-level term decomposition diagnostic before changing shader math.
  - Latest PBR response diagnostic update: expanded summary now reads renderer `mtoonLighting` metadata from RGBA JSON artifacts and prints `Renderer Lighting Metadata` plus `PBR Response Diagnostics`. Ash now emits the same top-level lighting metadata as wgpu/Bevy. Current `backpack_nm node145/mesh4/prim9/base` missing response is about `11-15x` the current `pbrAmbient=0.0318` in output-space normalized terms across all three Rust renderers, while `color_fit: null` alias fallback remains covered and absent from regenerated summary JSON. Treat this as a term-decomposition target, not a numeric ambient setting.
  - Latest PBR term-decomposition update: the expanded summary now filters PBR diagnostics through the `gltf_pbr` shading-model join and reports estimated ambient and non-ambient response over the selected manifest sample. The real Seed-san table now contains only `backpack_nm node145/mesh4/prim9/base`; estimated ambient is `0.03` but actual non-ambient is roughly `1.71-1.95` and three-vrm expected non-ambient is roughly `2.08-2.41`. Prioritize direct/specular/light accumulation parity against three.js `MeshStandardMaterial`; do not convert the previous missing-response number into an ambient or exposure knob.
  - Latest direct-term ratio update: the PBR response table now includes material context (`roughness/metallic/occlusion/normal-scale`) and non-ambient `E/A`, missing/actual, and direct-scale-needed RGB ratios. Current Seed-san `backpack_nm` is consistent across renderers but not a single neutral scalar: wgpu needs `1.22,1.25,1.26`, Bevy `1.21,1.24,1.24`, and Ash `1.19,1.22,1.23` over the estimated non-ambient term after subtracting ambient. Next concrete task is to decompose three.js `MeshStandardMaterial` vs Rust PBR into diffuse direct, specular direct, and light-color/color-management pieces for this draw; avoid "fixing" by fitting one global gain.
  - Latest focused PBR-term diagnostic update: `map-render-hotspots.rs` now writes `pbr_terms` for `gltf_pbr` candidates, and the focused/expanded summaries keep a compact row. The current Seed-san `backpack_nm` focused pixels report direct diffuse around `0.15-0.16`, specular around `0.02`, total direct around `0.17-0.18`, ambient around `0.01`, and total direct+ambient around `0.16-0.19` under `interpolated_vertex_no_normal_map`. Next step: add normal-map sampling to the CPU diagnostic and add/collect a matching three.js term dump for `MeshStandardMaterial`, then compare term-by-term before changing shader constants.
  - Latest probe-execution join update: `audit-owner-sample-execution.rs --texture-audit` now imports those `recommended_probes` and reports their concrete execution state. The existing `render-parity-seed-base-color-flat32-owner-sample-execution-audit` recipe refreshes both current and expanded texture audits first, then prints `Recommended Probe Execution` for each backend. The current expanded artifacts show the recommended probe draws are routed (`0` missing draw-selection entries), so the next implementation work should not chase manifest draw assignment. Prioritize material/color evaluation for routed `backpack_nm`/eye-style rows and owner/sample adoption or resolve shading behavior for `body_nm` sample-closer rows.
  - Latest shading-model/manifest-sample update: hotspot reports now preserve the material shading model, and the texture audit compares manifest-selected sample RGBA against actual/expected pixels. On expanded2 selected rows, `backpack_nm` is `gltf_pbr` for wgpu/Bevy/Ash, while `huku_bake`, `body_nm`, `arm_mat`, and `arm_plastic` are MToon. Bevy selected samples are actual-closer for `51/51` pixels with mean manifest A/E distance `0.6346 / 94.8525`, so Bevy is faithfully rendering the selected sample but not three-vrm's expected color. wgpu/Ash selected samples are less exact (`33/18/0` and `45/19/0` actual/expected/tie), so they still need color-output inspection too. Next concrete task: investigate glTF/PBR fallback color/output parity for `backpack_nm` separately from MToon local fill/triangle ownership for `body_nm`/plastic rows.
  - Latest glTF/PBR generated guard: `tools/render-parity/generate-gltf-pbr-fixture.rs` plus `just render-parity-gltf-pbr-generated` now cover non-MToon base color, texture, roughness, metallic, emissive strength, occlusion, unlit, texture-factor, and a Seed-san-shaped normalTexture path across wgpu, Bevy, and Ash. The current generated guard passes at `rgb-interior1px >= 47 dB`, max selected delta `<= 6`, and per-swatch `>= 40 dB` / max delta `<= 6`; selected PSNR is wgpu/Ash `47.8937 dB` and Bevy `47.4289 dB`. The `pbr-backpack-like` swatch combines mipped base texture, normal texture, roughness `0.657`, and single-sided rendering and is exact for all three Rust renderers, while the weaker rough/metal swatches remain around `41-42 dB`. `tools/render-parity/inspect-pbr-material.rs` and `just inspect-seed-pbr-material` record Seed-san `backpack_nm` as material `14`, `gltf_pbr`, base texture `backpack`, normal texture `nm_backpack_normals`, roughness `0.657`, and no broad extension surprises. This means the Seed-san `backpack_nm` blocker should be treated as real-model local sample/material-condition work rather than a broad glTF/PBR normal-map, mipped base-texture, or fallback failure.
  - Latest texture-as-linear diagnostic update: `just render-parity-seed-base-color-texture-as-linear-diagnostic` adds an explicit browser/wgpu/Bevy mode where base texture bytes are treated as linear before output encoding. The mode is internally consistent against its own three-vrm diagnostic (`35.3608 dB` wgpu / `35.2372 dB` Bevy on `rgb-shared-nonblack-interior1px`), but comparing Rust texture-as-linear output against the normal three-vrm base-color reference collapses to `16.6009 dB` wgpu / `16.6118 dB` Bevy. Keep `base-color-raw-srgb` as the raw-resource/manual-decode check and `base-color-texture-as-linear` as a diagnostic only; do not use either as a broad default color-space fix for `backpack_nm`.
  - Latest focused material-state update: focused and expanded summaries now join three-vrm browser `diagnosticMaterials` with Rust renderer `materialDraws`. Current `backpack_nm` focused rows show browser `MeshStandardMaterial`, `map=backpack`, `cs=srgb`, filter `1007/1006`, identity matrix, and color `1,1,1`, alongside Rust `backpack_nm` resolve draws with base texture slot `10` and normal texture slot `13`. Do not chase a broad material-name, base map colorSpace/filter, texture matrix, or base-color-factor mismatch for those rows; continue with local owner/surface/sample choice or narrower texture evaluation.
  - Latest gradient owner projection update: `just render-parity-seed-base-color-flat32-gradient-owner-hotspots` shows three-vrm renders a non-zero owner for `64/64` gradient hotspots, but only `27/64` match the CPU center frontmost candidate. Subpixel recovery reaches `49/64` with all recovered owners at depth rank `1`, and one-pixel neighbor recovery reaches `44/64` with mixed offsets. This suggests the next implementation work should model or reproduce subpixel/source-order ownership around high-gradient UV/material regions rather than applying a global pixel-center or adjacent-edge tweak.
  - Latest texture-filter diagnostic update: `--force-nearest-textures` is available across three-vrm, wgpu, Bevy, and `tools/ci/local-ci.rs`, with `just render-parity-seed-base-color-nearest-diagnostic` as the Seed-san smoke. The current nearest/no-mip run only raises the outline-off shared-body slice to wgpu `33.5501 dB` / Bevy `33.4815 dB`, while the gradient complement remains wgpu `27.1258 dB` / Bevy `27.1208 dB`. Do not treat broad mip/filter policy as the primary remaining Seed-san base-color fix unless a future fixture contradicts this.

## Ongoing Maintenance

- [ ] Integrate `vrm-rs-extension-kit` requirements manually and idiomatically.
  - Source archive: `D:\sanze\Downloads\vrm-rs-extension-kit.zip`.
  - Integration note: the archive's `tools/apply.py --dry-run` proposes useful patches, but the zip does not include the referenced `overlay/` crate/module sources. Do not apply the script blindly; port each requirement into the existing workspace with focused tests and normal gates.
  - [x] Expression conformance slice: binary expression threshold uses `> 0.5`, non-finite input becomes `0`, `block` fully suppresses a category when its source output is positive, `blend` scales by source output, binary targets collapse to `0` when category multipliers make them fractional, renderer-facing expression effects use the same rule, unknown expression writes have a checked API, and malformed VRM1 expression JSON is an error instead of being silently dropped.
  - [x] Structured diagnostics and strict/lenient load policy.
    - [x] Foundation: added `vrm-diagnostics` with typed severity, policy, JSON path, and report APIs, and connected VRM1 expression parsing so strict mode errors with structured diagnostics while lenient mode reports and skips malformed expressions.
    - [x] IO loader policy API: `vrm-io` now exposes `load_vrm_from_slice_with_policy` / `load_vrm_from_path_with_policy` and returns `LoadedVrmWithDiagnostics`, merging sans-IO diagnostics with IO warnings and unknown root-extension preservation warnings while keeping the existing strict loaders unchanged.
  - [x] Lossless source preservation for raw JSON, unknown extensions, extras, and GLB chunks.
    - [x] Foundation: `vrm-io::LoadedVrm` now exposes a `GltfSource` snapshot with original bytes, raw JSON bytes, parsed root JSON, root unknown extension/extras accessors, and preserved GLB JSON/BIN/unknown chunks.
    - [x] Writer acceptance hardening: `GltfSource` validates GLB declared length and chunk alignment up front, preserves unknown GLB chunks through metadata edits, and keeps root unknown extensions/extras intact in generated roundtrip coverage.
    - [x] Full metadata access: `GltfSource::vrm_full_metadata` returns typed VRM1/VRM0 protocol metadata plus the raw metadata JSON so applications can inspect complete author/license/permission fields and unknown raw fields without copying them into normalized runtime core.
  - [x] glTF/GLB/VRM writer plus metadata editing and atomic save.
    - [x] Foundation: `GltfSource` can patch VRM1/VRM0 metadata, re-encode `.gltf` or `.glb` while preserving non-JSON GLB chunks, and atomically save the edited or original source through a same-directory temp file.
    - [x] Writer options and metadata helpers: compact/pretty JSON output is selectable for `.gltf` and `.glb`, metadata patch byte helpers are public, and metadata patch atomic saves fail before touching the destination when the source shape is unsupported.
  - [x] Persistent runtime pipeline with LookAt, first-person controller, fixed-step spring stepping, and observable stage report.
  - [x] VRMA mixer with loop, seek, fade/crossfade, layers, masks, additive mode, and root-motion policy.
  - [x] VMC 3.1 typed messages, OSC conversion, bundle traversal, runtime sink, and transaction-safe application policy.
    - [x] Foundation: added `vrm-vmc` over `vrm-osc` with typed messages for availability, relative time, root/bone poses, blend values/apply, camera, directional light, unknown-message retention, OSC roundtrip helpers, recursive bundle traversal, parse-before-apply behavior, and transaction hooks with rollback on sink errors.
    - [x] Breadth pass: VMC now covers controller/key/MIDI input, HMD/controller/tracker device poses including local poses, receive/config/VRM/remote/settings/window/period/eye/calibration/shortcut messages, `/VMC/Thru/*` passthrough, official camera/light addresses with legacy alias parsing, and selectable strict vs invalid-message-skipping parse policy.
    - [x] Transport policy boundary: `VmcTransportGate` adds socket-free sender allow lists, per-sender rate limits, packet message limits, relative-time rewind/jump checks, and runtime sink application over the existing parse-before-apply transaction path while leaving UDP/TCP sockets and authentication sources to applications.
  - [x] MToon GPU ABI/reference WGSL plus practical Bevy/wgpu/ash resource and pipeline hookups.
    - `MtoonGpuUniform` / `MtoonGpuMaterial` are shared through `vrm-adapter`, `wgpu_mtoon_resource_plans` exposes wgpu-owned resource setup data, ash frame plans emit per-pipeline uniform uploads plus `AshGraphicsPipelinePlan` vertex/color/depth state, the unsafe ash example materializes offscreen graphics pipelines, uploads every MToon descriptor texture it can decode, creates uniform buffers/samplers/descriptor writes, and can submit/read back the rendered color target as checksum, `.rgba.json`, and direct `.imqraw` artifacts, and Bevy material plans carry the same uniform into engine-owned assets.
    - `AshDrawableFramePlan` now turns `AshRendererFrame` into a downstream-recordable command sequence with render pass metadata, coalesced pipeline/descriptor/buffer binds, indexed draws, and typed skipped-draw reasons. This closes the gap between resource materialization and practical ash command-buffer integration without moving Vulkan device ownership or unsafe calls into the library.
    - [x] Promote Ash from non-gating visual-review comparison into the same thresholded visual parity gate as wgpu/Bevy for the current six-fixture `rgb-visible >= 34 dB` sample floor. The external SPIR-V handoff, source-controlled MToon base shader compile/readback path, render-parity runner integration, scene/camera/light option sync, Vulkan clip-space Y flip plus CCW front-face policy, material-extra/UV-transform uniform surface, render-parity diagnostic modes, glTF emissive/occlusion texture-slot surface, outline geometry expansion and outline-pipeline routing, opaque-black clear-alpha parity, view-space matcap UVs, UV animation rotation parity, VRM model-orientation boundary, sRGB output correction, unlit/PBR fallback branches, v0-compatible direct clamp, generated tangent fallback for tangentless normal-map primitives, per-vertex normal-scale/double-sided state, slot-specific fallback textures, glTF sampler policy propagation, draw-order-sorted triangle owner-id diagnostic output, concrete glTF-only/PBR base pipelines, mip-chain texture materialization, and mask-alpha shader policy are available. `just render-parity-samples-ash-gated` now runs the six-fixture sweep with `--render-ash-readback --render-ash-visual-gate`; the latest local run passed with Ash selected PSNRs Seed-san `34.6391 dB`, Constraint `36.2509 dB`, MToon UV animation `35.6342 dB`, expression samples `55.6968` / `55.7181 dB`, and Alicia `35.6238 dB`, all with opaque-black alpha parity after the mask-alpha fix.
    - [x] Add a transparent-background Ash-gated real sweep at the current regression floor. `just render-parity-real-transparent-ash-gated` runs the same six real fixtures with `--render-background transparent`, `rgb-all >= 32 dB`, `--render-alpha-mismatch-tolerance 64`, and Ash included in `--render-ash-visual-gate`. The latest local run passed with Ash selected PSNRs Seed-san `34.6391 dB`, Constraint `36.2509 dB`, MToon UV animation `35.6342 dB`, expression samples `55.6968` / `55.7181 dB`, and Alicia `35.6238 dB`; Ash alpha mismatches stayed at Seed-san `25`, Constraint `11`, UV animation `0`, expression samples `3` / `3`, and Alicia `32`, matching the current allowed real-transparent floor.
    - [x] Add Ash to the focused real tangentless normal-map gate. `just render-parity-real-normal-maps-ash-gated` now runs Seed-san and `VRM1_Constraint_Twist_Sample.vrm` with `--render-ash-readback --render-ash-visual-gate`, `rgb-visible >= 34 dB`, exact alpha parity, and generated-tangent normal maps. The latest local run passed with Ash selected PSNRs Seed-san `34.6391 dB` and Constraint `36.2509 dB`, both with alpha mismatches `0`.
    - [x] Add Ash to the model-body nonblack diagnostic gate. `just render-parity-samples-nonblack-ash-gated` runs the six real fixtures with `rgb-nonblack-interior1px >= 27.4 dB`, exact alpha parity, and Ash in `--render-ash-visual-gate`. The latest local run passed; the low point is Seed-san Bevy `29.4512 dB`, with Seed-san Ash `30.1042 dB`, constraint Ash `29.7370 dB`, UV animation Ash `29.6052 dB`, expression samples Ash `51.4007 dB`, and Alicia Ash `34.4442 dB`.
    - [x] Add Ash to the stricter shared-body diagnostic gate. `just render-parity-samples-shared-body3-ash-gated` runs the six real fixtures with `rgb-shared-nonblack-interior3px >= 28.5 dB`, exact alpha parity, and Ash in `--render-ash-visual-gate`. The latest local run passed; the low point is the constraint sample at wgpu `28.6412 dB` / Bevy `28.6300 dB` / Ash `28.6392 dB`, with Seed-san Ash `31.9056 dB`, UV animation Ash `30.1907 dB`, expression samples Ash `51.6179 dB`, and Alicia Ash `33.1953 dB`.
    - [x] Add Ash to the constraint shared-body hotspot/pass diagnostic. `just render-parity-constraint-shared-body3-pass-summary-ash-gated` now derives shared-body deltas, CPU hotspot maps, and compact summaries for wgpu, Bevy, and Ash from the Ash-gated sweep. The current top-64 summaries show `64/64` visible frontmost candidates, `62-63/64` hotspots within `0.25px` of an internal edge, `51/64` actual-vs-frontmost pass matches, and `1/64` actual-vs-frontmost triangle matches for all three Rust renderers, so the floor is a shared local edge/material ownership problem rather than an Ash-specific backend path.
    - Remaining renderer-parity work is higher-than-34/32/27.4/28.5 final thresholds, broader official/third-party fixture coverage, model-body parity above the current floors, real tangentless normal-map fixture breadth beyond the current pair, and exact local edge/material/color residual cleanup.
  - [x] Optimizer for unused vertices, joint palette compaction, and empty morph removal.
    - Foundation: `vrm-io::optimize_primitive` validates primitive attribute/index shape, removes degenerate triangles, drops unreferenced vertices, normalizes skin weights, removes empty morph targets, compacts weighted joint palettes, and returns explicit vertex/joint remap reports. `apply_joint_compaction_to_skin` applies the same palette remap to glTF skin data, and the root facade re-exports the preprocessing API.
  - [x] Codec/resource registry for data URI/file resolution and Draco/Meshopt/KTX2 provider APIs with size/path safety.
    - [x] Foundation: `vrm-io::resource` now provides data URI and relative file resolution with path traversal rejection, file/data/decode size limits, missing-codec errors, and provider traits/registry for Draco, Meshopt, and KTX2/Basis-style texture decoding without bundling codec dependencies.
    - [x] Texture provider practicality: texture decode requests now carry source color space and renderer-facing decode options, GPU format capability helpers select BC7/ETC2/ASTC/RGBA fallback preferences, providers can override option-aware decode, and the registry rejects unsupported decoded texture formats after size-limit validation.
  - [x] Revisit extension-kit known limitations: VRM0 LookAt curve compatibility, transport socket/rate-limit policy boundaries, and whether `FullMeta` is needed without breaking existing API.
    - [x] VRM0 LookAt curve compatibility: `RangeMap` now preserves VRM0 Unity-style two-key Hermite curve data, sans-IO maps `FirstPersonDegreeMap.curve`, and runtime LookAt expression weights evaluate through the shared curve-aware range mapper.
    - [x] VMC transport/rate-limit boundary: `vrm-vmc` now provides reusable Sans I/O transport policy gating without owning sockets; authentication identity, network IO, retry, and source-of-trust decisions remain outside the crate.
    - [x] FullMeta decision: full metadata stays out of normalized runtime `Meta` to avoid a breaking core-model expansion; `VrmFullMetadata` exposes typed VRM1/VRM0 protocol meta and raw preserved JSON from `GltfSource`.
- [ ] Keep `docs/progress.md` as a chronological log.
- [ ] Keep `docs/testing.md` coverage numbers current after coverage-affecting work.
- [ ] Keep `docs/adapter-guide.md` aligned with public adapter APIs.
- [ ] Ask a subagent for pessimistic review after larger parity or adapter slices.
